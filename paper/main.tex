\documentclass{article}

% NeurIPS 2024 style (preprint mode via [final], matching the GAUSE companion)
\usepackage[final]{neurips_2024}

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{amsfonts}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{nicefrac}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{algorithm}
\usepackage{algorithmic}
\usepackage{xcolor}
\usepackage{multirow}
\usepackage{subcaption}
\usepackage{amsthm}
\usepackage{colortbl}

% Theorem environments
\newtheorem{proposition}{Proposition}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}
\newtheorem{assumption}{Assumption}
\newtheorem{observation}{Observation}

% Custom commands
\newcommand{\R}{\mathbb{R}}
\newcommand{\E}{\mathbb{E}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\TV}{\mathrm{TV}}
\newcommand{\KL}{\mathrm{KL}}
\newcommand{\nm}{\textsc{RISP}}
\newcommand{\regret}{\rho}
\newcommand{\Ginv}{G_{\mathrm{inv}}}
\newcommand{\Gforget}{G_{\mathrm{forget}}}
\DeclareMathOperator*{\argmax}{arg\,max}
\DeclareMathOperator*{\argmin}{arg\,min}

\title{When the Regime Returns:\\
Retention and Invariance in Decision-Focused Strategy Pools\\
for Non-Stationary Markets}

\author{
  Yuhao Li\\
  University of Pennsylvania \\
  \texttt{li88@sas.upenn.edu}
}

\begin{document}

\maketitle

\begin{abstract}
Financial markets are non-stationary in two distinct ways that existing
strategy-allocation methods conflate. \textbf{Between regimes}, market states
(trend, range, high-volatility, crisis) switch and recur on irregular
schedules with long dormancy, so a reward-chasing capital allocator---recent-P\&L
weighting or a learned mixture-of-experts router---starves and overwrites the
dormant-regime specialist and pays a full recalibration cost exactly when the
regime returns. \textbf{Within a regime}, each recurrence is a different draw
from the regime's environment distribution (the 2008 crisis is not 2020 is not
2022), so a specialist trained by empirical risk minimization on past episodes
of its own regime still fails out-of-distribution on the next one. We show
these failure modes are governed by \emph{independent} design choices and
propose \nm{} (Regime-Invariant Specialist Pools), a pool of capacity-bounded,
decision-focused specialists in which (i)~regimes are acquired by batched
winner-take-all competition, yielding a converged regime-to-specialist
assignment that is \emph{independent of current reward}---the crisis
specialist idles through calm markets and retains its calibration by identity,
not P\&L---and (ii)~each specialist trains its niche with a decision-focused
\emph{invariance} objective (variance plus mean of an SPO-style regret
surrogate across historical episodes of its regime). We prove a
post-reactivation regret bound that decomposes into a \emph{forgetting
deficit}---zero for any isolated covering assignment, approaching the full
relearning cost for any reward-driven one---and an \emph{invariance gap}
controlled by the episode-variance penalty, including the
Pinsker-type bridge from divergence-metric invariance to regret. In controlled
regime-switching markets with realistic signal-to-noise, the predicted
$2{\times}2$ dissociation holds with all pre-registered orderings confirmed
(100 seeds, Holm-corrected): emergent reward-independent assignment cuts
post-reactivation decision regret by $4.4\%$ versus a total-capacity-matched
learned router ($p{=}2.5{\times}10^{-7}$; $4.3\%$ versus the
per-specialist-matched monolith, $p{=}1.9{\times}10^{-7}$, capacity
accounting in \S\ref{sec:e1}), the invariance objective cuts it a
further $5.0\%$ ($p{=}5.8{\times}10^{-9}$; $9.1\%$ jointly,
$p{=}6.0{\times}10^{-23}$; $36\%$ of the excess over the converged
invariant-service floor), and the interaction is \emph{super-additive}: invariant training buys
nothing in a reward-driven monolith ($p{=}0.89$ vs.\ ERM), because an objective
cannot help a head that has been evicted. The learned router and the
recent-performance allocator inherit the monolith's failure; the full system
is statistically indistinguishable from a hand-pinned oracle assignment
trained with the same objective ($p{=}0.53$)---recovering the skyline without
being handed the assignment---and beats the oracle assignment
trained with ERM ($p{=}1.5{\times}10^{-6}$). We pair the
mechanism with an honest audit in the GAUSE tradition, organized as a
\emph{two-gate} deployment protocol whose gates are checkable before
any mechanism is bought (gate 1: regime structure on the decision
metric against shuffled labels; gate 2: a positive measured forgetting
deficit, monolith minus oracle-pinned). Daily crypto and commodity
panels fail gate 1 and dissociate null, as required; 4-hour crypto
passes gate 1 but measures a zero deficit and is null again; on
\emph{drawdown-labeled daily equities} (49 industry portfolios,
1990--2025, pre-registered) the deficit is \textbf{positive on real
data} ($+0.00089 \pm 0.00011$, 100 seeds; the interval is
implementation precision only, single history) and the full
pre-registered ordering emerges (headline
contrast Holm $p{=}5.2{\times}10^{-65}$; the ordering's binding pair
at $1.6{\times}10^{-15}$)---though at event level (27 reactivations)
the cell passes its registered Wilcoxon test ($p{=}0.016$) but
\emph{misses} the registered sign test ($p{=}0.061$, 18 of 27
positive), a lodged adverse branch we take in full. The record now
extends beyond one sample:
on a \textbf{withheld era} (36 industries, 1926--1989, frozen
specification), the deficit's \emph{sign} predicted the arm-ordering
direction in \textbf{5 of 6} pre-registered cells---in both
directions, including two positive-deficit cells reproducing the full
ordering out-of-sample (Holm $1.9{\times}10^{-6}$,
$2.1{\times}10^{-4}$; seed-precision registers---at event level, per
the N1 battery, one cell is Wilcoxon-only and the other fails both
registered tests)---while gate 1's conditioning criterion tracked
neither and is demoted to a structure screen. A first non-US cell
(25 Japanese size--value portfolios, 1990--2025, frozen
specification) resolves the lodged region-specific sign prediction
as a hit: $\hat\Gamma$ negative-significant with the
sign-rule-predicted
inversion realized (Holm $3.9{\times}10^{-8}$), in a market whose
crisis-union occupancy ($67\%$) is flagged occupancy-anomalous---the
out-of-sample sign-rule record stands at 9 of 10 cells, both
directions represented. The pooled cross-history event-level
register (109 reactivations, independent histories counted once,
Japan's negatives signed) then split the claim's registers: its
sign test \emph{fails} ($p{=}0.063$)---the frequency form of the
deficit claim is retired program-wide---its Wilcoxon passes
($p{=}0.0086$), and its dollar-weighted register sums to
$+4{,}274$ bps-days with a bootstrap CI covering zero, so the
economic claim is \emph{region-conditional}, priced by the
cell-level sign rule rather than pooled-positive. Transaction costs
\emph{amplify} rather than kill the effect on 1990--2025
(net-of-cost deficit
$+0.00126 \to +0.00230$ as crisis-window costs sweep 25--100\,bps;
the one observed inversion, under the vol-band labeler, reverses net
of costs), though the amplification is era-local: on the withheld
era the net deficit survives only the 25 bps register---a cost
level anachronistic for 1926--89, disclosed---fading at the
era-realistic 50--100 bps tiers, with the crisis-churn turnover
signature absent, while the pool's net edge over the monolith
survives every tier; a decoupled-replay monolith closes only $56\%$ of the
deficit in the controlled market and, on equities, collects the
deficit (${\approx}96\%$ at 100 seeds) yet still loses to the
emergent pool (paired $p{=}2.7{\times}10^{-12}$), while an
expanding-window retrainer that never discards data collects only
$59\%$ and leaves a real $+3.7$ bps/day residual; and the
deficit is not an artifact of the discrete memory model---a shared-trunk
MLP with nothing evicted reproduces it as pure gradient interference
($+0.01009 \pm 0.00037$, 100 seeds). One attribution is honestly
revised: on real data the deficit neither decays across the probe
window nor follows any dormancy or rehearsal covariate---it behaves as
a persistent \emph{regime-conditional allocation deficit}, not
demonstrated eviction-forgetting, which remains established only in
the controlled settings. The first externally-lodged temporal
test---an announcement-lagged NBER-recession labeler with no free
threshold, its registration deposited with the Open Science Framework
before the recession dates were ever joined to the panel---has since
resolved as a hit under both scoring forms
($\hat\Gamma = +0.00081 \pm 0.00033$ with the ordering at Holm
$1.6\times10^{-4}$, cost-amplified, and both registered event-level
tests passed---sign $p{=}0.021$, Wilcoxon $p{=}0.016$; its
schedule-resampling counterpart flat, disclosed). Caveats carried at
equal prominence: each walk-forward window is a single market history
(three independent market histories are now scored---US 1990--2025,
US 1926--1989, Japan---with the NBER labeler a second lodged test on
the first), the deficit's paying threshold is a
window, not a point---the lodged fine map finds windows on both
eras overlapping at $13$--$16\%$ under exit hysteresis, superseding
an earlier relocation reading (on the withheld era the window exists
only under exit hysteresis, and that era's lodged $10\%$ anchor
missed its sub-criterion; the committed window-location
estimator failed its first calibration, disclosed pre-CRSP and
retired),
aggregation itself can manufacture slow dynamics, and the
constituent-level CRSP test---its registration lodged with OSF
before any data access---remains the decider. All claims are
regret and retention claims, not alpha claims; code, data, and
per-figure result files are released.
\end{abstract}

%=============================================================================
\section{Introduction}
%=============================================================================

A multi-strategy fund maintains playbooks for market conditions that are
mostly absent: the crisis desk idles through years of calm; the
short-volatility book hibernates through stress. The allocation question that
governs such a system---\emph{which strategy gets capital, attention, and
recalibration today, and who keeps a strategy whose regime is dormant?}---has
a standard machine-learning answer (route by recent reward: a learned gate, a
recent-Sharpe weighting) and a standard failure mode that the GAUSE companion
study isolated in controlled prediction domains \citep{li2026gause}: a dormant
regime emits no reward, so any allocator that protects capacity using current
reward is structurally blind to exactly the knowledge that most needs
protection. When the regime returns, the system relearns what it once knew,
and in markets the relearning window---the first days of a crisis---is
precisely when decisions are most consequential.

This paper develops that observation into a theory and system for
\emph{decision-focused} learning in non-stationary markets, and in doing so
isolates a second, orthogonal failure mode that the retention story alone
misses. Suppose the crisis specialist \emph{is} protected---its parameters
untouched through dormancy. Protection guarantees only that the specialist
still knows \emph{the last crisis}. Each occurrence of a regime is a distinct
draw from that regime's environment distribution: flight-to-quality
correlations, volatility clustering, and liquidity spirals recur
\emph{structurally}, but the surface features that carried them in 2008
differ from 2020 and from 2022. A specialist trained by empirical risk
minimization (ERM) on its own regime's history loads on episode-specific
("spurious") regularities and fails out-of-distribution on the next episode
\citep{arjovsky2019irm, yan2026invpnco}. Retention solves \emph{between-regime}
forgetting; it does nothing for \emph{within-regime} drift.

Our central claim is that these are \textbf{two orthogonal kinds of
non-stationarity}, addressed by \emph{independent} mechanisms:

\begin{itemize}
    \item \textbf{(N1) Between-regime switching} is an \emph{allocation}
    problem. It is solved by any capacity assignment that \emph{isolates}
    the dormant specialist---no foreign updates during its regime's
    dormancy---while persistently covering the regime inventory, so that
    the specialist retains by identity; converged reward-independent
    competition is one way to obtain such isolation, and reward-chasing
    allocation destroys it. No training objective can substitute: a
    forgotten head is gone regardless of the loss it was trained with.
    \item \textbf{(N2) Within-regime drift} is a \emph{training-objective}
    problem. It is solved by an invariance objective across the regime's
    historical episodes---penalizing the variance of a decision-focused
    (SPO-style) regret surrogate across episodes so the specialist learns the
    decision-relevant structure common to \emph{all} crises rather than the
    idiosyncrasies of the last one. No allocation mechanism can substitute:
    retention of an episode-overfit head retains the overfitting too.
\end{itemize}

The retention half of this division has a recent formal ancestor in the
average-reward account of continual learning \citep{kumar2025continual},
which argues that a computationally bounded agent should forget freely
whatever will not recur---forgetting non-recurring information is not
catastrophic---and that even recurring information need not be retained
when relearning on arrival is cheap: their capacity-bounded agent facing
a two-season market relearns each season's function as it arrives. Our
deficit $\hat\Gamma_{\mathrm{forget}}$ (Contribution C4) is a
decision-regret operationalization of exactly that trade-off for
markets---a per-substrate measurement of whether the regime inventory
recurs in a way a bounded learner is paying to relearn---and
Proposition~\ref{prop:breakeven} prices their relearn-quickly proviso.

The paper's single falsifiable claim is the resulting $2{\times}2$:
crossing allocation mechanism (reward-driven vs.\ reward-independent) with
training objective (ERM vs.\ invariant) should produce monotone improvement
along both axes in post-reactivation decision regret, with a signature
asymmetry---the invariance axis should be inert in the reward-driven row,
because an evicted head cannot benefit from the objective that trained it.
Our experiments confirm every cell of this prediction, including the
asymmetry (Section~\ref{sec:e1}), and our theory derives it
(Section~\ref{sec:theory}).

\paragraph{Scope statement (the sleeping-experts deflation, addressed up
front).} The contribution concerns capacity-bounded \emph{learners}, not
combinations of fixed experts. Sleeping-experts and adaptive-regret schemes
\citep{freund1997sleeping, hazan2009adaptive, daniely2015strongly} also
freeze dormant experts' state---but only because their experts are
\emph{static strategies} with nothing to forget. The problem \nm{} solves
arises precisely when specialists must keep \emph{learning} within their
niche under bounded capacity, so that reward-driven reallocation causes
destructive interference across regimes. We operationalize this distinction
as a baseline pair: Hedge over \emph{fixed} strategies retains trivially but
cannot adapt within-regime and is dominated by every learning arm
(Section~\ref{sec:e1}); Hedge over capacity-bounded \emph{learning} experts
inherits the interference failure and tracks the monolith. The most dangerous
related work thus becomes a supporting experiment.

\paragraph{Contributions.}
\begin{enumerate}
    \item \textbf{Problem decomposition (C1).} We formally decompose
    non-stationary financial decision error into a between-regime
    \emph{forgetting deficit} and a within-regime \emph{invariance gap}, and
    show---in theory and experiment---that they are governed by independent
    design choices (allocation vs.\ training objective), with a predicted and
    confirmed super-additive interaction: invariant training pays \emph{only}
    inside a retaining allocation.
    \item \textbf{Method (C2).} \nm{}: the first strategy-pool architecture
    combining reward-independent, emergent (competition-driven) capacity
    assignment with decision-focused invariant learning per niche. The
    anticipated financial adaptation of the GAUSE competition mechanism is a
    \emph{batched} winner-take-all window ($W_c$ days of cumulative
    within-regime regret); we pre-registered the expectation that per-step
    competition would converge to noise at realistic signal-to-noise, and
    report honestly that the expectation is \emph{not} borne out---the
    exponentiated-gradient affinity accumulation across windows performs the
    necessary averaging, and the mechanism is robust across
    $W_c \in \{1, \dots, 60\}$ (Section~\ref{sec:e5}). No gate is trained,
    no freezing schedule is set, no regime-to-desk map is designed.
    \item \textbf{Theory (C3).} A post-reactivation regret bound at fixed
    tail level,
    $\E_A[\mathrm{CVaR}_\delta[\regret_{\mathrm{react}}]] \le
    \Ginv(\delta) + \Gforget$
    (Proposition~\ref{prop:decomp})---allocation controls the mean of the
    reactivation transient, the invariance objective its tail across
    episode draws, a tail reading whose pre-registered quantile test
    subsequently \emph{failed}, demoting it to an unconfirmed bound
    (see after Proposition~\ref{prop:decomp})---in which $\Gforget = 0$ for \emph{any}
    isolated covering assignment while reward-driven assignment
    drives it to the full relearning cost at an explicit rate
    (Proposition~\ref{prop:dichotomy}); $\Ginv(\delta)$ is controlled by
    the episode-variance penalty (Lemma~\ref{lem:transfer}), with an explicit
    Pinsker-type bridge from divergence-metric invariance statements to the
    regret metric (Lemma~\ref{lem:bridge})---closing the technical gap
    between Inv-PnCO-style guarantees \citep{yan2026invpnco} and decision
    regret. A break-even analysis (Proposition~\ref{prop:breakeven}) gives
    the dormancy window in which retention pays once idle-specialist carrying
    costs are charged.
    \item \textbf{Evaluation protocol (C4).} A diagnostic-first
    standard for regime-aware financial ML:
    \emph{post-reactivation regret} in a probe window after each
    reactivation, leakage-free causal regime labelings, and a
    pre-deployment protocol run \emph{before} claiming gains --- a
    structure \emph{screen} plus a binding diagnostic. We state the
    protocol as its own record left it, not as designed: the screen's
    outcomes have not tracked pool value in any tested cell (four
    dissociations, including its strongest pass coinciding with an
    inversion), so it screens for learnable structure and licenses
    nothing; the diagnostic is the measured deficit. \emph{Screen}
    (structure): regime-conditioning must beat block-shuffled labels on
    the decision metric. \emph{Gate 2} (harvestable deficit): the
    measured deficit---monolith minus oracle-pinned post-reactivation
    regret---must be positive. The decomposition of
    Proposition~\ref{prop:decomp} bounds this quantity's
    \emph{transient} branch (eviction-relearning), which is the
    measured mechanism in our controlled settings; on the real
    substrate where the probe profile shows no decay
    (Section~\ref{sec:e1f}), $\hat\Gamma$ measures a persistent
    quantity no proposition in this paper bounds---we say so rather
    than borrow the theorem's authority. The record across
    the real substrates (Sections~\ref{sec:e0}--\ref{sec:e1f}): daily
    crypto/commodities fail gate 1 and dissociate null, as required;
    4-hour crypto passes gate 1, fails gate 2, dissociates null; on
    daily equities (1990--2025) the deficit's \emph{sign} predicted the
    walk-forward outcome in both tests---zero deficit $\Rightarrow$ the
    monolith wins significantly (the invariance premium with no claim),
    positive deficit $\Rightarrow$ the full pre-registered ordering
    emerges---and on a withheld 64-year era (1926--1989, frozen
    specification) the same sign rule scored 5 of 6 pre-registered
    cells, in both directions. Japan, the first non-US cell, then
    scored 4 of 4 under a lodged region-specific prediction (a
    correctly priced inversion), taking the out-of-sample record to
    9 of 10; the pooled cross-history event-level register keeps the
    ledger honest---the frequency form of the claim is retired, and
    the economics are region-conditional, priced by the rule cell by
    cell. The protocol was revised by its own
    record---gate 2 was added after one refutation, and gate 1, whose
    strongest pass in the program ($z{=}23.5$, 1926--1989) coincides
    with an \emph{inverted} pool ordering, is demoted to a structure
    screen: it certifies that regime labels carry decision-relevant
    information, and it does not price the pool. What survives is a
    single-number deployment rule, $\hat\Gamma_{\mathrm{forget}}$ from
    the A1-vs-A9 counterfactual---now tested out-of-sample in both
    directions and robust to measured crisis-window transaction
    costs---whose forward test on CRSP constituents is pre-committed.
    Where the deficit is not positive, no retention
    mechanism (ours included) can be bought, and a method that had
    claimed otherwise would be fitting noise.
    \item \textbf{Honest audit (C5).} Conditions under which \nm{} does
    \emph{not} pay, each quantified: slack capacity ($K \to R$ closes the
    monolith gap); high within-regime signal-to-noise (a fresh learner
    recalibrates within days, making retention worthless---we sweep the
    break-even relearning speed); short dormancy (the retention gap grows
    with dormancy length); structureless regimes (the E0 gate); and carrying
    costs above the break-even threshold. And where the deficit \emph{is}
    positive on real data, we do not claim its mechanism: the measured
    quantity behaves as a regime-conditional \emph{allocation} deficit
    (persistent, not decaying; Section~\ref{sec:e1f}), and
    eviction-forgetting proper is demonstrated only in the controlled
    settings (E1 and Section~\ref{sec:e1m}).
\end{enumerate}

We emphasize register throughout: our claims are \emph{regret and retention}
claims about a mechanism, demonstrated where its preconditions verifiably
hold, not alpha claims about live markets. The honest summary of the real
data is that the two preconditions are \emph{separable}, fail separately,
and---on the substrates where both the structure and a positive
measured forgetting deficit coexist (drawdown-labeled daily industries,
Section~\ref{sec:e1f})---jointly deliver the pre-registered ordering in
single-history walk-forwards, at aggregate (portfolio) level, an effect
that survives and strengthens under measured crisis-window transaction
costs: no decision-metric structure on small daily panels (gate 1), no
harvestable forgetting on the intraday substrate where structure exists
(gate 2), an inversion where the deficit is zero or negative, the
dissociation where it is positive, and a 5-of-6 out-of-sample scorecard
for that sign rule on a withheld 64-year era, extended to 9 of 10 by
Japan's correctly predicted inversion---each finding surfaced by
the diagnostic-first protocol, the first consistent with the GAUSE
oracle audit on the prediction metric \citep{li2026gause}. The
pooled cross-history register bounds the claim's register: the
frequency form (``the typical reactivation pays'') is retired, and
the economics are region-conditional---the sign rule prices cells,
in both directions, and a pooled headline cannot.

%=============================================================================
\section{Related Work}
%=============================================================================

\paragraph{Regime-switching finance.} Markov-switching models
\citep{hamilton1989new}, regime effects in asset returns
\citep{ang2002international, ang2012regime}, regime-conditioned allocation
\citep{guidolin2007asset, nystrup2018dynamic}, and statistical jump models
for persistent market states \citep{nystrup2020greedy} detect regimes and
condition a \emph{single} model on them. None address bounded capacity or
dormancy-driven forgetting of the specialist itself: the implicit assumption
is that conditioning information is free to retain. Regime-switching
forecast combination \citep{elliott2005optimal} conditions the
combination \emph{weights} on an inferred regime but likewise assumes
every component model remains fully estimated while its regime is
dormant: the regime moves the weights, never the fitness of the
experts; the regime-conditional allocation deficit of
Section~\ref{sec:e1f} measures precisely that assumption's failure
under bounded capacity. Our monolith arm
(regime-conditioned, capacity-bounded, always trained on the active regime)
is this literature's best case under a capacity budget, and the comparison
isolates what conditioning alone cannot provide.

\paragraph{Decision-focused learning (predict-then-optimize).} The SPO
framework and its convex surrogate \citep{elmachtoub2022smart}, surrogate
relaxations for hard combinatorial problems \citep{mandi2020smart}, learned
decision losses \citep{shah2022lodl}, and the field survey
\citep{mandi2024decision} all assume a stationary (or one-shot OOD)
environment. Inv-PnCO \citep{yan2026invpnco} introduces invariant learning
for predict-and-optimize under distribution shift, bounding the divergence
between solution distributions across environments. We adapt its
environment-indexed invariance to \emph{recurring} regimes with
dormancy---episodes of a regime as environments---and contribute the
bridge from divergence-metric guarantees to regret
(Lemma~\ref{lem:bridge}), plus the orthogonal allocation axis that the
stationary setting never encounters. Online decision-focused learning
has since acquired its first regret guarantees under time-varying
distributions \citep{capitaine2026online}; that analysis treats drift
as unstructured, whereas our setting's difficulty is \emph{recurrence
with dormancy}---the property that makes retention a design axis at
all---which drift-only regret bounds do not touch.
Closest in application is ReCAP \citep{pan2026recap}, which builds the
retention machinery end to end for portfolio management: change-point
detection on market-state features segments history into variable-length
regimes, each distilled into a policy-difference vector stored in a
library that is frozen at deployment (only a state-driven softmax gate
and the current regime's vector keep training), so that a returning
regime is served by re-weighting stored vectors. This is
parameter-isolation retention realized by design---detected regime
identity playing the role task identity plays in the PackNet family
above---evaluated by the standard averaged forgetting/transfer metrics
rather than post-reactivation decision regret, with no retention
counterfactual. ReCAP therefore \emph{presumes} the question our
protocol makes primary: it deploys retention unconditionally, with no
pre-deployment test of whether the substrate offers a harvestable
deficit. The diagnostic of Section~\ref{sec:e1f} prices exactly such
machinery before it is bought---and our record contains real substrates
where the sign is negative and an always-on retention stack pays the
premium with no claim to collect.

\paragraph{Online learning and ensembles.} Hedge/multiplicative weights
\citep{freund1997decision, arora2012multiplicative}, universal portfolios
\citep{cover1991universal}, online portfolio selection \citep{li2014online},
and the sleeping-experts / adaptive-regret line
\citep{freund1997sleeping, hazan2009adaptive, daniely2015strongly} are the
closest formal relatives. The scope statement of Section~1 is our
differentiation: these schemes combine \emph{fixed} experts, for which
dormancy is harmless by construction. When the experts are capacity-bounded
\emph{learners}, the combination scheme's weights are reward-driven and the
training resource follows the weights---reintroducing exactly the
interference that retention requires avoiding. The tracking line sharpens
the point: Fixed Share \citep{herbster1998tracking} and especially mixing
past posteriors \citep{bousquet2002mixing}, which attains logarithmic
regret against a small set of \emph{returning} experts (see also
time-selection regret \citep{blum2007external}), solve the recurrence
problem for the combiner's \emph{weights}---a returning expert is
re-elevated almost instantly. They cannot solve it for the expert's
\emph{parameters}: under bounded capacity the training resource follows
the weights, so the dormant specialist that mixing past posteriors would
re-elevate has meanwhile been overwritten---MPP retains the pointer, not
the playbook, and our A8b arm equipped with Fixed-Share weighting would
inherit the same interference. Our A8a/A8b baseline pair
tests both readings directly.

\paragraph{Mixture-of-experts and continual learning.} MoE theory for
continual learning \citep{li2024moecl} shows isolation mitigates forgetting
\emph{provided} experts are plentiful and the gate's updates are eventually
terminated. Recent routing-stabilization work reduces router-induced
expert churn during \emph{active} training (CP-MoE \citep{liu2026cpmoe};
StaR-MoE \citep{guo2026starmoe}), but leaves the dormancy problem
untouched---a regime that emits no reward for hundreds of steps gives a
stabilized router nothing to stabilize toward---and neither evaluates
post-reactivation regret, the metric on which the failure appears.
GAUSE \citep{li2026gause} showed empirically that in the excluded
regime (bounded capacity, gate still learning) a reward-driven router
inherits the monolith's forgetting, and that emergent competitive exclusion
reaches the required reward-independent assignment with no freezing schedule.
We import that mechanism as a premise, port it to decision-focused financial
learning (batched competition, regret-based quality), and extend the theory
from prediction error to decision regret. Among classical
continual-learning remedies, regularization approaches (EWC
\citep{kirkpatrick2017overcoming}; survey \citep{parisi2019continual})
operate within a single model, but the parameter-isolation
family---PackNet \citep{mallya2018packnet}, HAT \citep{serra2018hat},
SupSup \citep{wortsman2020supsup}, Expert Gate
\citep{aljundi2017expertgate}, PathNet \citep{fernando2017pathnet},
progressive nets \citep{rusu2016progressive}---already realizes
reward-independent capacity assignment when task identity is given (or,
for SupSup, inferred at test time), and
is the continual-learning instantiation of the isolation property of
Proposition~\ref{prop:dichotomy}. What that family does not provide is
the allocation-under-competition analysis---which reward-driven
assignment destroys isolation, at what rate, and how a covering
assignment emerges without a designed task-to-mask map---nor the
within-regime invariance axis, which is orthogonal to isolation and is
where the super-additive interaction lives. Continual learning has also
been proposed for investment decision-making directly
\citep{philps2018continual}, without either analysis.

\paragraph{Multi-strategy practice and strategy decay.} The practitioner
analogue of our mechanism is the siloed desk structure of multi-strategy
funds; the practitioner analogue of our failure mode is recent-P\&L capital
allocation \emph{across} desks. Strategy decay \citep{mclean2016does} and
backtest-overfitting critiques \citep{bailey2014deflated, harvey2016backtesting,
lopez2018advances} motivate both our within-regime drift axis and our
statistical protocol (deflated statistics, no test-period tuning,
controlled semi-synthetic ground truth).

%=============================================================================
\section{Problem Setup and Method}
\label{sec:setup}
%=============================================================================

\subsection{Data stream, episodes, and the decision layer}

A stream $t = 1, \dots, T$ presents observable per-asset features
$X_t \in \R^{n \times d}$ and an unknown coefficient vector $y_t \in \R^n$
(next-period returns, $\|y_t\|_\infty \le y_{\max}$), with a latent regime
$r_t \in \{1, \dots, R\}$ evolving in persistent blocks. Regimes recur:
the $e$-th maximal active spell of regime $r$ is \emph{episode} $(r, e)$,
a stationary environment with law $P_{r,e}$ over $(x, y)$ pairs; the
dormancy $D(r)$ of a regime is the length of the inactive spell between
consecutive episodes. Crisis-type regimes have long, irregular dormancy and
few historical episodes---the regime that most needs a calibrated specialist
is the one observed least and least recently.

\begin{definition}[Decision layer and regret]
\label{def:regret}
The feasible set is the cardinality-constrained long-only portfolio
$Z = \{ z \in [0, w_{\max}]^n : \sum_i z_i \le W,\ \|z\|_0 \le k \}$ with
linear utility $F(z, y) = y^\top z$. The plug-in policy decides
$z(\hat y) = \argmax_{z \in Z} \hat y^\top z$ (exactly solvable: $w_{\max}$
on the $k$ largest positive coordinates when $W \ge k\, w_{\max}$), and the
per-step \emph{decision regret} is
\[
  \regret(\hat y; y) \;=\; F(z^*(y), y) - F(z(\hat y), y) \;\in\; [0, B],
  \qquad B = 2 k\, w_{\max}\, y_{\max}.
\]
\end{definition}

Regret---not prediction error---is the object of interest: a predictor can
have large mean-squared error and zero regret (errors that do not change the
top-$k$ ranking are free) and small error but large regret (errors
concentrated at the selection boundary). This is the core predict-then-optimize
lesson \citep{elmachtoub2022smart, mandi2024decision} and the reason the
structure diagnostic of Section~\ref{sec:e0} must be re-run under the
decision metric rather than inherited from prediction-metric audits.

Specialists are linear decision-focused predictors per regime
(\emph{heads}): head $w \in \R^d$ predicts $\hat y = X_t w$ and trains on
the SPO+ surrogate \citep{elmachtoub2022smart} of
Definition~\ref{def:regret}'s regret, whose subgradient requires only two
calls to the decision oracle per sample. Episode risk is
$L_{r,e}(w) = \E_{P_{r,e}}[\regret(Xw; y)]$.

\subsection{Capacity and memory models}
\label{sec:memory}

Each specialist maintains at most $K < R$ regime heads, under two memory
models spanning the discrete and graded readings of capacity
\citep{li2026gause}:
\textbf{hard (LRU)}: learning a non-held regime at full capacity evicts the
least-recently-used head (reset to prior), modeling slot-like capacity
(an analyst's active playbooks, a finite adapter store);
\textbf{soft (interference)}: a shared backbone with per-regime adapters in
which every foreign update decays untouched adapters' effective knowledge by
a factor $(1 - \rho_{\mathrm{int}})$, modeling parameter interference in
shared representations. Episode buffers (bounded: the most recent
$E_{\mathrm{buf}}$ episodes, $\le 40$ days each) count toward the same
budget and are evicted with their head: knowledge lost is data lost, not
merely parameters reset.

\subsection{The \nm{} mechanism}
\label{sec:mechanism}

\nm{} runs $N$ specialists through a batched competition cycle
(Algorithm~\ref{alg:risp}):

\begin{enumerate}
    \item \textbf{Shadow evaluation.} Every specialist produces (shadow)
    decisions for the current regime $r_t$; per-specialist decision regret
    accumulates over a rolling \emph{competition window} of $W_c$ trading
    days within the regime.
    \item \textbf{Batched winner-take-all.} At window close, the
    \emph{window winner} minimizes cumulative window regret plus a niche
    shaping term $-\lambda(\alpha_{i, r_t} - 1/R)$; only the winner applies
    one exponentiated-gradient (Hedge) update to its niche affinity
    $\alpha_i \in \Delta^R$ \citep{kivinen1997exponentiated,
    freund1997decision} and trains on the window's data. Batching is the
    financial adaptation: per-period regret differences are noise-dominated
    at realistic signal-to-noise, and a $W_c$-day cumulative comparison
    drives the wrong-winner probability down exponentially in $W_c$
    (Remark~\ref{rem:batching}); $W_c \in \{1, 5, 20, 60\}$ is ablated in
    Section~\ref{sec:e5}.
    \item \textbf{Invariant niche training.} A specialist updating regime
    $r$ minimizes, over its episode buffer for $r$,
    \begin{equation}
    \label{eq:invloss}
        \mathcal{L}_r(w) \;=\; \Var_{e}\!\big[\bar\ell_{r,e}(w)\big]
        \;+\; \beta\, \E_{e}\!\big[\bar\ell_{r,e}(w)\big],
    \end{equation}
    where $\bar\ell_{r,e}$ is the mean SPO+ surrogate on stored episode $e$:
    the regime-indexed Inv-PnCO objective \citep{yan2026invpnco,
    krueger2021rex} with episodes as environments. ERM ablations minimize
    the mean alone.
    \item \textbf{Pinning.} When a specialist's affinity for a regime
    exceeds $0.95$, the assignment is \emph{pinned}: the owner alone serves
    and trains that regime thereafter, making the converged assignment
    reward-independent---and, because the owner alone trains its niche,
    isolated---by construction (competition re-opens only if the
    regime inventory changes).
\end{enumerate}

\begin{algorithm}[t]
\caption{\nm{} (batched competition cycle)}
\label{alg:risp}
\begin{algorithmic}[1]
\REQUIRE $N$ specialists with capacity $K$, regimes $\{1..R\}$, window $W_c$,
EG rate $\eta$, niche bonus $\lambda$, invariance weight $\beta$
\STATE Initialize $\alpha_{i} \leftarrow \mathbf{1}/R$; heads empty; no pins
\FOR{$t = 1, 2, \dots, T$}
    \STATE Observe $X_t$, regime label $r_t$ (causal labeler); serve decision
    of pinned owner if $r_t$ pinned, else of $\argmax_i \alpha_{i, r_t}$
    \IF{$r_t$ pinned}
        \STATE Owner stores $(X_t, y_t)$ in episode buffer; SGD steps on
        Eq.~\eqref{eq:invloss}
    \ELSE
        \STATE Accumulate window regret $q_i \mathrel{+}= \regret_i$ for all
        $i$; buffer the day
        \IF{window length $= W_c$ or regime block ends}
            \STATE $i^* \leftarrow \argmax_i \big[-q_i / |\text{win}| +
            \lambda(\alpha_{i, r_t} - 1/R)\big]$
            \STATE EG update: $\alpha_{i^*, r_t} \mathrel{*}= e^{\eta}$;
            renormalize $\alpha_{i^*}$
            \STATE $i^*$ trains on the window's data
            (Eq.~\eqref{eq:invloss}); LRU/interference bookkeeping
            \STATE \textbf{if} $\alpha_{i^*, r_t} \ge 0.95$: pin $r_t \to i^*$
        \ENDIF
    \ENDIF
\ENDFOR
\end{algorithmic}
\end{algorithm}

\paragraph{Regime labeling.} All arms (ours and baselines) receive the same
causal regime label $r_t$, computed from information through $t-1$ only:
\textbf{L1} (headline), a rolling-percentile volatility band crossed with
trend sign; \textbf{L2}, a causal two-state filtered volatility model
(forward-only Hamilton-style filter \citep{hamilton1989new}) crossed with
trend sign. The comparison is therefore information-symmetric
(task-incremental in continual-learning terms \citep{li2026gause}): what
differs across arms is solely whether the induced capacity assignment chases
current reward, not what each is told.

\paragraph{Episode scarcity.} Crisis regimes yield 5--7 historical episodes
in long equity samples and fewer in shorter panels;
$\Var_e[\cdot]$ over so few draws is fragile. Two mitigations are part of
the default design: \emph{sub-episode environments} (non-overlapping blocks
within episodes enlarge the environment set while preserving
between-episode heterogeneity) and \emph{cross-asset episodes} (the same
crisis in different asset classes counts as distinct environments, upgrading
the claim to cross-asset invariance within a regime). Our synthetic
heterogeneity sweep (Section~\ref{sec:e4}) controls the episode count
directly; the estimation price of few episodes appears explicitly in
Lemma~\ref{lem:transfer}.

%=============================================================================
\section{Theory: A Decomposition of Post-Reactivation Regret}
\label{sec:theory}
%=============================================================================

We state the assumptions, the two lemmas that control each axis, and the
propositions that assemble them. Proofs not given inline appear in the
companion deep-dive document; all results are idealized-model statements in
the register of the GAUSE theory \citep{li2026gause}---guides to mechanism,
with the quantitative claims resting on the controlled experiments. We do
\emph{not} claim adversarial-regret guarantees for the coupled
winner-take-all dynamics.

\begin{assumption}[Episode exchangeability; regime-indexed invariance]
\label{ass:hyper}
For each regime $r$, episode environments are i.i.d.\ draws
$\xi_{r,1}, \xi_{r,2}, \dots \sim Q_r$ from a regime-level
hyper-distribution, and the deployed (next) episode is a fresh draw. For a
fixed head $w$ define $\mu_r(w) = \E_{\xi \sim Q_r}[L_\xi(w)]$ and
$\sigma_r^2(w) = \Var_{\xi \sim Q_r}[L_\xi(w)]$.
\end{assumption}

This is the regime-indexed form of the Inv-PnCO environment assumption
\citep{yan2026invpnco}: ``the next crisis is a new draw from the crisis
distribution.'' It idealizes on two counts---real episodes are neither
independent (macro memory) nor exchangeable (secular drift)---and we flag
both in Section~\ref{sec:limitations}.

\begin{assumption}[Dormancy and foreign updates]
\label{ass:dormancy}
While regime $r$ is dormant for $D$ steps, an allocation mechanism $A$
routes learning updates among specialists. Under a \emph{reward-driven} $A$,
the specialist holding $r$ is made to learn a currently active regime with
per-step probability at least $p > 0$ (the \emph{marginal-$p$ form}:
active regimes are the only sources of reward, reward attracts capacity,
and the allocator spreads its updates across the active regimes rather
than fixating on one). Call an assignment \emph{isolated} for $r$ if the
owner of $r$ receives no updates \emph{other than to $r$ itself} during
$r$'s dormancy---under the hard model this excludes acquiring foreign
heads (eviction pressure), and under the soft model it excludes
\emph{any} non-$r$ update, since each one decays $r$'s adapter by the
interference factor---and \emph{covering} if every regime has an owner
within capacity. (The stronger reading matters: a specialist holding
$\{r, r''\}$ with $r''$ active during $r$'s dormancy is isolated in the
hard sense but not in the soft sense, and carries a positive soft-model
deficit accordingly.)
Isolation-plus-coverage is the operative property throughout;
\emph{reward-independence}---an assignment measurable with respect to
information independent of the realized reward stream (pinned converged
competition, fixed niches, a frozen gate, hand-designed silos)---is the
mechanism route by which our arms obtain isolation, not the property
itself (Remark~\ref{rem:isolation}).
\end{assumption}

\begin{remark}[Isolation, not reward-independence, does the work]
\label{rem:isolation}
Reward-independence is neither necessary nor sufficient for isolation: a
round-robin trainer is reward-independent and covering yet not
isolated---every specialist receives foreign updates on schedule, so its
forgetting deficit is positive---while a hand-designed silo is isolated
regardless of whether its designer consulted past rewards. The phrase
``reward-independent assignment'' \citep{li2026gause} names our mechanism
class; the property invoked by the theory below is isolation plus
coverage.
\end{remark}

\subsection{The invariance axis}

\begin{lemma}[Episode transfer]
\label{lem:transfer}
Under Assumption~\ref{ass:hyper}, for any fixed $w$ and $\delta \in (0,1]$,
with probability $\ge 1 - \delta$ over the test episode $e' \sim Q_r$,
\[
  L_{r, e'}(w) \;\le\; \mu_r(w) + \sigma_r(w)\sqrt{(1-\delta)/\delta}
  \;\le\; \mu_r(w) + \sigma_r(w)/\sqrt{\delta}.
\]
Moreover, with $E_r$ training episodes and $L \in [0, B]$, the empirical
mean satisfies $|\hat\mu_r(w) - \mu_r(w)| \le B \sqrt{\log(2/\delta) /
(2 E_r)}$ with probability $\ge 1 - \delta$ (Hoeffding over episodes), and
an analogous $O(B/\sqrt{E_r})$ bound holds for the empirical standard
deviation via empirical-Bernstein concentration
\citep{maurer2009empirical}.
\end{lemma}

\begin{proof}
The first display is Cantelli's one-sided inequality applied to the random
variable $L_\xi(w)$, $\xi \sim Q_r$: $\Pr[L_\xi \ge \mu_r + \tau] \le
\sigma_r^2 / (\sigma_r^2 + \tau^2)$, solved at the $\delta$ level. The
second is Hoeffding's inequality for $[0, B]$-bounded i.i.d.\ variables.
\end{proof}

The \nm{} objective (Eq.~\ref{eq:invloss}) is, up to the $O(B/\sqrt{E_r})$
estimation terms, a Lagrangian scalarization of the bound's two arguments:
sweeping $\beta$ traces the $(\mu_r, \sigma_r)$ Pareto frontier, and for
each tail level $\delta$ some $\beta(\delta)$ optimizes the bound. ERM
ignores $\sigma_r$ entirely and can sit far up the variance axis---the
mechanism by which it loads on episode-specific regularities
(Section~\ref{sec:e4} constructs this failure explicitly). We are explicit
about register: the trained $\hat w$ depends on the training episodes, so
applying Lemma~\ref{lem:transfer} to $\hat w$ is a plug-in heuristic at the
same level as Inv-PnCO's use of its Theorem~1, not a uniform-convergence
guarantee; the estimation terms are the formal price of few episodes.

\begin{lemma}[Divergence-to-regret bridge]
\label{lem:bridge}
For any episode laws $P, P'$ on $\mathcal{X} \times \mathcal{Y}$ and any
measurable head $w$,
\[
  |L_P(w) - L_{P'}(w)| \;\le\; B \cdot \TV(P, P')
  \;\le\; B \sqrt{\KL(P \,\|\, P') / 2}.
\]
Consequently, if a representation renders episode laws
$\varepsilon$-invariant in KL (as in Inv-PnCO-style guarantees, which bound
divergences between solution distributions \citep{yan2026invpnco}), then
$\sigma_r(w) \le B\sqrt{\varepsilon / 2}$ for heads factoring through it,
and Lemma~\ref{lem:transfer} applies with that variance.
\end{lemma}

\begin{proof}
Regret is $[0, B]$-valued pointwise (Definition~\ref{def:regret}), so the
difference of its expectations under $P, P'$ is at most $B \cdot \TV$ by
the variational characterization of total variation applied to
$g = \regret / B \in [0,1]$; Pinsker's inequality
\citep{tsybakov2009introduction} gives $\TV \le \sqrt{\KL/2}$.
\end{proof}

This closes the announced technical gap: invariance statements in the
divergence metric transfer to the regret metric at the explicit price of the
regret range $B = 2 k\, w_{\max}\, y_{\max}$ (loose---empirical regret
ranges are roughly fifty times smaller---but explicit; the decomposition
statement below survives verbatim in the KL metric if the constant is
objectionable).

\subsection{The retention axis}

\begin{lemma}[Eviction rate under reward-driven allocation]
\label{lem:eviction}
Hard memory model, capacity $K$, Assumption~\ref{ass:dormancy} with $W_a \ge
K$ distinct active regimes during the dormancy of $r$. Let $U_D$ be the
number of distinct non-held foreign regimes the owner of $r$ is made to
learn during $D$ dormant steps. Then $r$'s head is evicted on the event
$\{U_D \ge K\}$, and
$\Pr[U_D < K] \le \Pr\big[\mathrm{Binom}(D,\, p\,(W_a - K + 1)/W_a) < K\big]
\to 0$ exponentially in $D$. Under the soft model, the retained strength of
$r$ after $N_D$ foreign updates is $(1 - \rho_{\mathrm{int}})^{N_D} \to 0$.
\end{lemma}

\begin{proof}
Each dormant step independently delivers a foreign-regime update with
probability $\ge p$; conditional on an update, a not-yet-held distinct
regime is hit with probability $\ge (W_a - K + 1)/W_a$ while fewer than $K$
distinct regimes have been accumulated. The waiting time to $K$ distinct
hits is thus stochastically dominated by a sum of $K$ geometric variables,
giving the binomial tail; LRU evicts $r$ once $K$ distinct foreign heads
displace it. The soft case is immediate from once-per-update multiplicative
decay.
\end{proof}

\begin{proposition}[The eviction dichotomy: isolation zeroes the
forgetting deficit]
\label{prop:dichotomy}
Define the forgetting deficit $\Phi(D; A) = \Pr[\text{$r$'s head lost at
reactivation}]$ (hard) or $1 - \E[(1-\rho_{\mathrm{int}})^{N_D}]$ (soft).
Under Assumption~\ref{ass:dormancy}: (i) for any reward-driven $A$ in the
marginal-$p$ form, $\Phi(D; A) \to 1$ as $D \to \infty$ at the rate of
Lemma~\ref{lem:eviction}---reward-driven allocation destroys isolation;
(ii) for any \emph{isolated covering} $A$, $\Phi(D; A) = 0$ for all $D$:
the owner of $r$ applies zero foreign updates during dormancy, so its
head---and its episode buffer---are exactly preserved (hard) or decay
only with the regime's own drift (soft). \nm{}'s pinned converged
competition is one emergent way to obtain isolation and coverage.
\end{proposition}

\begin{proof}
(i) is Lemma~\ref{lem:eviction}. (ii): isolation gives $U_D = 0$
deterministically---no eviction and no interference decay is ever
triggered---and coverage guarantees the owner exists and holds $r$
within capacity.
\end{proof}

The two regimes are different \emph{functions} of $D$---one tends to $1$,
the other is identically $0$---which is what makes the dichotomy
experimentally sharp (Section~\ref{sec:e3}). Direction (i) carries a
disclosed boundary: the proof uses Assumption~\ref{ass:dormancy}'s
marginal-$p$ form, in which the reward-driven allocator spreads across
the active regimes; a degenerate allocator that \emph{fixates} on a
single active regime never accumulates the $K$ distinct foreign heads
that LRU eviction requires and can accidentally retain. Reward-chasing
destroys isolation in the typical, spreading case---not by logical
necessity---and our reward-driven arms all satisfy the marginal-$p$ form
by construction. Coverage is the second conjunct, inherited from GAUSE:
random fixed niches are isolated for the regimes they happen to hold but
can leave a regime unowned, retaining only what they cover; emergent
competition guarantees coverage at $N \ge R$ \citep{li2026gause}, which we
re-verify in our setting (every seed of the 20-seed assignment diagnostic
converges to a one-per-regime covering assignment; Section~\ref{sec:e1}).

\subsection{Assembly}

\begin{proposition}[Post-reactivation regret decomposition]
\label{prop:decomp}
Under Assumptions~\ref{ass:hyper}--\ref{ass:dormancy}, let regime $r$
reactivate after dormancy $D$ into a fresh episode $e' \sim Q_r$, served
by allocation $A$ with the owner's head $\hat w_r$ trained on $E_r$
episodes. Let $\mathrm{CVaR}_\delta^{\,e'}$ denote the mean of the worst
$\delta$-fraction of outcomes over the episode draw $e'$, and let
$C_{\mathrm{relearn}}$ denote the probe-window regret \emph{excess} of a
from-prior head over the converged retained benchmark $\hat w_r$ (an
empirical constant of the learning problem)---so the eviction branch's
regret is the retained benchmark's regret \emph{plus}
$C_{\mathrm{relearn}}$, not $C_{\mathrm{relearn}}$ alone. Then, for any
tail level $\delta \in (0, 1]$,
\[
  \E_{A}\Big[\mathrm{CVaR}_{\delta}^{\,e'}
  \big[\regret_{\mathrm{react}}(r)\big]\Big]
  \;\le\;
  \underbrace{\mu_r(\hat w_r)
  + \sigma_r(\hat w_r)\sqrt{\tfrac{1-\delta}{\delta}}
  + O\!\Big(\frac{B}{\sqrt{E_r}}\Big)}_{\textstyle \Ginv(\delta)}
  \;+\;
  \underbrace{\Phi(D; A)\, C_{\mathrm{relearn}}}_{\textstyle \Gforget},
\]
where the outer expectation is over the allocation and eviction
randomness and the inner tail functional is over the fresh episode draw.
At $\delta = 1$ the statement specializes to the expectation bound
$\E[\regret_{\mathrm{react}}(r)] \le \mu_r(\hat w_r) + O(B/\sqrt{E_r})
+ \Gforget$, in which the $\sigma_r$ term vanishes.
\end{proposition}

\begin{proof}
Pointwise in both sources of randomness, the accounting inequality is
$\regret_{\mathrm{react}}(r) \le L_{r,e'}(\hat w_r) +
\mathbf{1}\{\text{evict}\}\, C_{\mathrm{relearn}}$: on retention the
served head is $\hat w_r$; on eviction the served head is fresh, and its
probe-window regret is the retained benchmark's regret on the same draw
plus $C_{\mathrm{relearn}}$, by the definition of $C_{\mathrm{relearn}}$
as an excess. The eviction event is a function of the dormancy-period
allocation stream and is independent of $e'$ under
Assumption~\ref{ass:hyper}. Conditional on that event, bound the first
term by the CVaR form of Cantelli's inequality---for any random variable
with mean $\mu$ and variance $\sigma^2$, $\mathrm{CVaR}_\delta \le \mu +
\sigma\sqrt{(1-\delta)/\delta}$, the same one-sided moment argument
whose quantile version is Lemma~\ref{lem:transfer}---plus
Lemma~\ref{lem:transfer}'s $O(B/\sqrt{E_r})$ estimation price; the
second term is constant given the event. Take the outer expectation over
the eviction indicator, $\E[\mathbf{1}\{\text{evict}\}] = \Phi(D; A)$.
\end{proof}

The registers of the two terms carry the interpretation: allocation
controls the \emph{mean} of the reactivation transient ($\Gforget$ is an
expectation over the eviction randomness and survives at every
$\delta$), while the invariance objective controls its \emph{tail
across episode draws} (the $\sigma_r$ term enters only for
$\delta < 1$). We state the flip side honestly, because it sharpens
rather than weakens the claim: in pure expectation ($\delta = 1$) the
$\sigma_r$ term vanishes and the population-level bound is minimized by
the mean-optimal (ERM) head---the variance penalty purchases
distributional robustness against the next episode draw, not
population-mean improvement, and its empirical mean advantage in E1
operates through the finite-episode estimation term, where ERM loads on
episode-specific regularities (the E4 construction).

The tail half of that interpretation was put to its pre-registered
test (D5) and \emph{failed}, demoting it to an \emph{unconfirmed
bound} per the lodged branch: on the 100-seed synthetic battery the
A5$-$A6 gap does not concentrate in the upper quantiles of
per-reactivation regret (gap-to-median ratios $1.09$ at q75, $0.68$
at q90, $0.43$ at CVaR@$10\%$, all Holm-n.s., min corrected
$p = 0.090$; the CVaR point estimate leans \emph{opposite} the
prediction, excess gap $-0.00115 \pm 0.00103$)---the invariance
effect appears empirically as a uniform shift across quantiles of
the reactivation transient, and the $\sigma_r$ mechanism, while it
bounds the tail, is not established as the operative one.

\begin{proposition}[Independent controls; the $2{\times}2$ prediction]
\label{prop:additive}
$\Gforget$ depends only on $(A, D, K, \text{memory model})$ and is zeroed
by an isolated covering assignment regardless of the training
objective; $\Ginv(\delta)$ depends only on $(\text{objective}, Q_r, E_r)$
and, at every tail level $\delta < 1$, its $\sigma_r$ term is reduced by
the variance-penalized objective regardless of the assignment.
The bound is therefore additive in the two design axes, predicting monotone
improvement along both axes of the $2{\times}2$. Moreover the realized
interaction is predicted \emph{super-additive} in one specific cell: under
a reward-driven $A$ with $\Phi \approx 1$, the served head at reactivation
is fresh regardless of its training objective, so the invariance axis is
inert in the reward-driven row ($\Ginv$ improvements act on a head that is
not there to serve).
\end{proposition}

\begin{remark}[Honest register]
Additivity of the \emph{bound} does not force additivity of realized
regret; we therefore measure the empirical interaction index
$I = (\text{A1} - \text{A5}) + (\text{A1} - \text{A7}) - (\text{A1} -
\text{A6})$ and report it with the headline (Section~\ref{sec:e1}). The
super-additivity prediction---invariance inert in the reward-driven
row---is the distinctive falsifiable signature separating our account from
a generic ``both tricks help'' story.
\end{remark}

\begin{proposition}[Break-even dormancy]
\label{prop:breakeven}
Charge a carrying cost $c$ per idle specialist-day (infrastructure,
attention, calibration operations) and let $V$ scale one unit of
probe-window regret to portfolio value over the $N_p$-day probe. Retention
of regime $r$'s specialist pays over a dormancy-reactivation cycle iff
\[
  c \cdot D \;\le\; \Phi(D; A_{\mathrm{rd}})\; C_{\mathrm{relearn}}\,
  N_p\, V,
\]
with $\Phi$ the reward-driven deficit of Lemma~\ref{lem:eviction}. Since
$\Phi$ saturates at $1$ while $cD$ grows linearly, the paying region is a
\emph{window} $[D_{\min}, D_{\max}]$: dormancy long enough that eviction is
near-certain, short enough that cumulative carrying cost does not swamp the
one-shot reactivation saving. Both endpoints are computed from measured
quantities in Section~\ref{sec:e3}.
\end{proposition}
% TODO(restructure): six-line capacity-constrained service-gap
% proposition (persistent branch of the deficit), anchored on
% kumar2025continual, so both branches of the sign rule are creatures
% of the same theory.

\begin{remark}[Batching the competition: a prediction revised by experiment]
\label{rem:batching}
The window winner is decided by a $W_c$-day cumulative regret comparison.
For two specialists with true within-regime quality gap $\Delta$ and
per-day comparison noise $\sigma_n$ (sub-Gaussian), Hoeffding gives
$\Pr[\text{wrong winner in one window}] \le \exp(-W_c \Delta^2 /
2\sigma_n^2)$, which motivated our pre-registered expectation that per-step
competition ($W_c = 1$) would converge to noise at realistic daily
signal-to-noise. The experiment revises this: the assignment is decided not
by any single window but by the \emph{accumulation} of EG updates across
many windows, and that accumulation performs the averaging the single-window
analysis assigns to batching---post-reactivation regret is flat across
$W_c \in \{1, 5, 20, 60\}$, with $W_c = 1$ marginally best
(Section~\ref{sec:e5}). The single-window bound is correct but answers the
wrong question; the operative statistic is the win-rate differential
integrated by the affinity dynamics. We retain $W_c = 20$ as the default for
interpretability (one affinity update per fortnight is auditable) and
report the sweep.
\end{remark}

%=============================================================================
\section{Experiments}
\label{sec:experiments}
%=============================================================================

\subsection{Protocol, arms, and metrics}
\label{sec:protocol}

\paragraph{Design philosophy.} Every claim is pre-registered as an ordering
prediction before the run, tested across 100 seeds with Welch tests and
Holm--Bonferroni correction (the controlled battery E1--E6 and the stitched
daily panel of Section~\ref{sec:e1s}; the intraday runs of
Sections~\ref{sec:e0} and~\ref{sec:e1r} use 20 seeds as run), and reported
whether or not it survives---including
two predictions that did \emph{not} (the batching expectation,
Remark~\ref{rem:batching}, and the real-data dissociation,
Section~\ref{sec:e0}). All numbers trace to released per-experiment result
files. We use three data tiers, in decreasing order of experimental control
and increasing order of realism: (i) \emph{synthetic} regime-switching
markets with known ground truth (the headline, because only here is every
causal claim testable); (ii) \emph{semi-synthetic} regime-stitched real
data (real per-regime cross-sections re-arranged into controlled dormancy
schedules); (iii) \emph{real walk-forward} panels (five Bybit USDT crypto
pairs, daily, 2021--2025; FRED commodities, 2015--2025; and, added in
revision, the Ken French industry portfolios, daily---49 industries
1990--2025 plus a withheld-era replication on the 36 complete
industries 1926--1989, Section~\ref{sec:e1f}---portfolio-level equity
evidence). We state
plainly that the roadmap's equities flagship (CRSP S\&P~500
\emph{constituents}, 1990--2025, with 5--7 crisis episodes) remains the
pre-committed forward test: the industry-portfolio results of
Section~\ref{sec:e1f} are facts about the aggregate cross-section, and
aggregation itself can manufacture slow dynamics
\citep{lo1988stock}---the constituent-level test is where that
interpretation is decided.

\paragraph{Synthetic market.} $n = 20$ assets, $R = 4$ regimes (three
common, one rare ``crisis''), $d = 20$ features per asset: two
\emph{invariant} dimensions carrying the regime's decision-relevant
structure $\theta_r$ (distinct, well-separated across regimes, stable
across episodes), one \emph{spurious} dimension whose loading
$\gamma_{r,e}$ is redrawn each episode (the episode ``weather''), sixteen
distractors, and a constant. Signal-to-noise is calibrated to realistic
daily cross-sections ($|\theta_r| \approx 0.012$ against noise
$0.02$--$0.06$; crisis noise $3\times$ calm), which makes fresh relearning
genuinely slow---the regime in which the allocation question is
non-trivial, and, as our audit shows (Section~\ref{sec:e6}), the regime in
which it \emph{stays} non-trivial. Common regimes alternate in blocks of
$25$--$60$ days; the crisis regime appears for $15$--$30$ days roughly
every fourth cycle, giving dormancy spells of $250$--$450$ days and $15$
evaluated reactivations per run ($T \approx 2{,}370$; first half burn-in,
second half evaluated). Decision layer: $k = 4$ of $20$, $w_{\max} = 0.25$.

\paragraph{Arms.} All learner arms share the identical specialist class
(linear SPO+ heads, capacity $K = 2$ of $R = 4$ unless swept, identical
learning rates and buffers); they differ \emph{only} in allocation and
objective. Capacity accounting: pool arms field $N = 4$ specialists
($4 \times K{=}2$ heads in total) while monolith arms run a single
specialist, so the equal-total-capacity allocation contrast is A5 vs.\
A2, with the monolith contrasts audited against the E2 capacity sweep
(Section~\ref{sec:e2}). A1 capacity-$K$ monolith (ERM; always trains the active
regime---the canonical reward-driven allocation and the regime-conditioned
finance baseline under a capacity budget). A2 mixture-of-experts with a
gate trained on realized regret (reward-driven routing). A3
recent-performance capital allocator: capital shares softmax-weight
trailing 60-day performance; each specialist trains on the active regime
with probability equal to its capital share (capital is recalibration
intensity---the industry baseline; its dormant-specialist erosion rate is
governed by the softmax floor). A4 random fixed niches
(reward-independent, coverage gaps possible). A5 \nm{}-ERM (emergent
assignment, ERM training). A6 \nm{}-full (emergent assignment, invariant
objective, Eq.~\ref{eq:invloss}). A7 monolith with the invariant objective
(tests whether the objective can substitute for allocation). A8a Hedge
over fixed strategies; A8b Hedge over capacity-bounded learning experts
(training follows the top weight)---the adaptive online-learning pair of
the scope statement. A9 oracle-pinned assignment with ERM; A10
oracle-pinned with the invariant objective (the skyline: hand-given
perfect assignment, nothing to discover).

\paragraph{Metrics.} \emph{Post-reactivation regret}: mean daily decision
regret over the first $15$ days of every block whose regime was dormant
$\ge 90$ days ($\ge 80$ in the stitched-crypto schedules, whose blocks
are shorter; the probe window mirrors the GAUSE protocol).
\emph{Overall}: mean daily regret over the evaluation half. \emph{Steady}:
within-block regret beyond the probe window (the converged-service level).
Where percentages of ``excess'' regret are quoted, the floor is the
steady-state regret of the A10 skyline ($0.0252$)---the best converged
\emph{regime-faithful} service in this market, not an irreducible bound:
episode-chasing arms trade \emph{below} it in steady state (A7 $0.0243$,
A8b $0.0244$, A1 $0.0245$) by fitting episode-specific signal, and pay
for it at reactivations---the E6 trade-off at baseline SNR. We always
quote raw numbers alongside, and we account for this steady-state tax
explicitly in Section~\ref{sec:e1}.

\subsection{E1 --- the headline $2{\times}2$ dissociation}
\label{sec:e1}

\begin{table}[t]
\centering
\small
\caption{E1: post-reactivation, overall, and steady-state decision regret
(mean $\pm$ 95\% CI over 100 seeds; synthetic market, $K{=}2$, hard memory).
Horizontal rules group: skylines / \nm{} / retaining baselines /
reward-driven arms / adaptive pair / replay monoliths. Pre-registered ordering confirmed:
$\text{A6} < \text{A5} \lesssim \text{A4} < \text{A1} \approx \text{A2}
\approx \text{A7} < \text{A3}$, with A8a worst overall and A8b tracking the
monolith. The A1r replay rows (added in revision, pre-registered D1) are
from the replay battery run under identical conventions
(\texttt{results/e1\_replay.json}, 100 seeds), whose re-run of the shared
arms reproduces every value shown here to the quoted precision.}
\label{tab:e1}
\begin{tabular}{lccc}
\toprule
\textbf{Arm} & \textbf{Post-react.} & \textbf{Overall} & \textbf{Steady} \\
\midrule
A10 oracle assignment + INV (skyline) & $0.0306 \pm 0.0004$ & $0.0261 \pm 0.0002$ & $0.0252$ \\
A9 \phantom{0}oracle assignment + ERM & $0.0322 \pm 0.0004$ & $0.0275 \pm 0.0003$ & $0.0263$ \\
\midrule
\rowcolor{blue!6}
A6 \phantom{0}\nm{}-full (emergent + INV) & $\mathbf{0.0308 \pm 0.0004}$ & $0.0262 \pm 0.0002$ & $0.0252$ \\
A5 \phantom{0}\nm{}-ERM (emergent + ERM) & $0.0324 \pm 0.0004$ & $0.0276 \pm 0.0003$ & $0.0265$ \\
\midrule
A4 \phantom{0}random fixed niches (ERM) & $0.0332 \pm 0.0005$ & $0.0285 \pm 0.0005$ & $0.0274$ \\
\midrule
A1 \phantom{0}monolith (ERM) & $0.0339 \pm 0.0004$ & $0.0268 \pm 0.0002$ & $0.0245$ \\
A7 \phantom{0}monolith (INV) & $0.0339 \pm 0.0004$ & $0.0266 \pm 0.0002$ & $0.0243$ \\
A2 \phantom{0}MoE router (regret-trained gate) & $0.0339 \pm 0.0004$ & $0.0271 \pm 0.0002$ & $0.0249$ \\
A3 \phantom{0}recent-performance allocator & $0.0375 \pm 0.0004$ & $0.0294 \pm 0.0002$ & $0.0267$ \\
\midrule
A8a Hedge over fixed strategies & $0.0440 \pm 0.0006$ & $0.0384 \pm 0.0003$ & $0.0372$ \\
A8b Hedge over learning experts & $0.0338 \pm 0.0004$ & $0.0267 \pm 0.0002$ & $0.0244$ \\
\midrule
A1r monolith + decoupled replay (ERM) & $0.0329 \pm 0.0004$ & $0.0277 \pm 0.0002$ & $0.0264$ \\
A1r$^{+}$ monolith + replay (INV) & $0.0314 \pm 0.0004$ & $0.0264 \pm 0.0002$ & $0.0252$ \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[t]
    \centering
    \begin{subfigure}{0.44\textwidth}
        \includegraphics[width=\linewidth]{figures/fig1_2x2.pdf}
        \caption{The $2{\times}2$: the invariance axis is inert in the
        reward-driven row (the predicted asymmetry).}
        \label{fig:2x2}
    \end{subfigure}\hfill
    \begin{subfigure}{0.54\textwidth}
        \includegraphics[width=\linewidth]{figures/fig7_trace.pdf}
        \caption{Daily regret aligned on reactivation.}
        \label{fig:trace}
    \end{subfigure}
    \caption{\textbf{Left:} post-reactivation decision regret across
    allocation $\times$ objective (mean $\pm$ 95\% CI, 100 seeds). Cells:
    A1 / A7 (top), A5 / A6 (bottom). \textbf{Right:} mean daily regret
    around reactivations (5-day MA, 95\% CI): the monolith pays a
    relearning transient concentrated in the probe window; \nm{}-full
    tracks the oracle-assignment skyline from day one.}
    \label{fig:headline}
\end{figure}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.86\textwidth]{figures/fig2_arms.pdf}
    \caption{E1, all eleven arms (synthetic, $K{=}2$, hard memory, 100
    seeds). Color encodes class: green = emergent reward-independent +
    invariant; blue = reward-independent (ERM or fixed); orange/red =
    reward-driven; black = oracle skyline. The dissociation separates
    classes, not architectures: every reward-chasing arm clusters with the
    monolith regardless of its machinery.}
    \label{fig:arms}
\end{figure}

Table~\ref{tab:e1} and Figures~\ref{fig:headline}--\ref{fig:arms} give the
headline. Every pre-registered prediction is confirmed
(Holm-corrected Welch tests over 100 seeds):

\textbf{The retention axis is real.} Emergent reward-independent
assignment with identical ERM training cuts post-reactivation regret from
$0.0339$ (A2, the regret-trained router) to $0.0324$ (A5), $-4.4\%$,
$p = 2.5 \times 10^{-7}$ (Holm $1.5 \times 10^{-6}$). We headline the
router contrast because it is the clean allocation-only comparison: A2
and A5 deploy identical total capacity ($N{=}4$ specialists of $K{=}2$
heads each) and differ solely in whether the assignment chases reward.
The monolith comparison is confirmatory but not pure: A1 matches the
per-specialist capacity, not the total head count, so its identical gap
($0.0339$ vs.\ $0.0324$, $p = 1.9 \times 10^{-7}$, Holm
$1.3 \times 10^{-6}$) partially reflects total heads---E2's hard
monolith at $K{=}4$ reaches $0.0322 \approx$ A5's $0.0324$
(Section~\ref{sec:e2}). Random fixed niches (A4) trail A5 ($0.0332$ vs.\
$0.0324$; at 100 seeds the gap reaches significance, $p = 0.011$, Holm
$0.044$) and carry $1.4\times$ its seed standard deviation---the signature
of coverage
gaps: a seed whose random draw covers all four regimes retains like A5; a
seed with an unowned regime pays for it on every reactivation.

\textbf{The invariance axis is real---inside a retaining allocation.}
Holding the emergent assignment fixed, the episode-variance objective cuts
post-reactivation regret from $0.0324$ (A5) to $0.0308$ (A6), $-5.0\%$,
$p = 5.8 \times 10^{-9}$ (Holm $4.7 \times 10^{-8}$). Jointly, A6 improves
on A1 by $9.1\%$ raw ($p = 6.0 \times 10^{-23}$), which is $36\%$ of the
excess over the converged invariant-service floor
($0.0339 - 0.0252 = 0.0087$ down to $0.0056$).

\textbf{The steady-state tax, charged in full.} The probe-window gain is
not free. At steady state the plain monolith trades \emph{below} the
invariant arms (A1 $0.0245$ vs.\ A6 $0.0252$, ${\approx}{+}0.0007$/day
against A6) by chasing episode-specific signal---the same behavior E6
shows becomes catastrophic at high SNR, profitable here at baseline.
Over this market's schedule (${\approx}225$ probe days vs.\
${\approx}960$ steady days in the evaluation half) the two effects
largely net out, which is why A6's \emph{overall} margin over A1 is
$2.1\%$ ($0.0262$ vs.\ $0.0268$), an order smaller than the
post-reactivation headline. We state this plainly rather than let the
probe metric carry it silently: the mechanism's value is concentrated
at regime onsets---which is where financial decisions are most
consequential, and which is the pre-registered reason post-reactivation
regret is the primary metric---and a deployment whose loss function
weights all days equally should expect the overall number, not the
probe number.

\textbf{The interaction is super-additive, exactly as
Proposition~\ref{prop:additive} predicts.} The invariant objective in a
reward-driven monolith (A7) is indistinguishable from ERM (A7 vs.\ A1
post-reactivation: $p = 0.89$; the equivalence also holds positively
under a paired TOST with margin $\pm 0.0008$---pre-declared as half the
smallest confirmed effect in this family, the A5-to-A6 invariance gap
$|\Delta| = 0.0016$---at $p = 9.6 \times 10^{-29}$): an objective cannot
help a head that was evicted during dormancy. Per-seed, the interaction index
$I = (\text{A1}{-}\text{A5}) + (\text{A1}{-}\text{A7}) -
(\text{A1}{-}\text{A6}) = -0.0017 \pm 0.0002$ (95\% CI excluding zero):
the joint improvement exceeds the sum of the marginal improvements because
the invariance margin only exists where retention preserves the head it
trained. This asymmetry is the distinctive signature of the two-mechanism
account---a generic ``both tricks help additively'' story would put $I$
at zero, and a shared-cause story would make A7 help. (A7 is directionally
better than A1 \emph{overall}---$0.0266$ vs.\ $0.0268$, not
significant---consistent with the objective helping while heads are alive
and being erased at each eviction.)

\textbf{Reward-driven arms cluster together, as the theory says they
must.} The learned router tracks the monolith ($p = 0.88$; TOST
equivalence at the same $\pm 0.0008$ margin, $p = 8.2 \times 10^{-12}$);
Hedge over
learning experts tracks it too ($0.0338$); the recent-performance
allocator is \emph{worst} among learner arms ($0.0375$)---it not only
forgets dormant regimes but trains specialists on a capital-weighted blend
of foreign regimes throughout, paying interference even in steady state.
The sleeping-experts reading of our problem is answered constructively:
A8a (fixed strategies) retains trivially yet is dominated by every
learning arm everywhere ($0.0440$ post-reactivation, $0.0372$ steady)---
retention without within-niche learning is worth little; A8b (learning
experts) learns but forgets---adaptivity of the \emph{combination} does
not protect the \emph{experts}. The contribution lives exactly in the gap
this pair brackets.

\textbf{\nm{} matches the skyline it was never given.} A6 is
statistically indistinguishable from A10---the hand-pinned oracle
assignment with the same invariant objective ($0.0308$ vs.\ $0.0306$,
$p = 0.53$; TOST equivalence at margin $\pm 0.0008$,
$p = 2.6 \times 10^{-12}$)---and significantly better than the oracle assignment with ERM
training (A9, $p = 1.5 \times 10^{-6}$, Holm $7.6 \times 10^{-6}$). As in
GAUSE,
the value of competition is not beating hand-assignment given the labels
(it cannot); it is \emph{recovering the oracle partition automatically}:
in every seed of the 20-seed assignment diagnostic (not rerun at 100
seeds) the population converges to a covering one-per-regime
assignment (the three common regimes pin at affinity $\ge 0.95$; the
rare crisis regime reaches $0.67$--$0.87$---short of the pin threshold
because crisis windows are scarce---but with the correct unique owner
serving throughout, an honest wrinkle we report rather than hide).

\begin{figure}[t]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig12_replay.pdf}
    \caption{Replay with an eviction-decoupled buffer, both substrates
    (post-reactivation regret, mean $\pm$ 95\% CI). Left: synthetic
    (100 seeds) --- the replay monolith closes $56.3\%$ of the deficit,
    below the pre-registered $60\%$, and remains far from A6. Right:
    French 49 L3 walk-forward (20-seed battery shown; the quoted
    statistics are superseded by the 100-seed K2 re-measurement of
    Section~\ref{sec:e1f}) --- replay collects the deficit
    (matching A9) yet the invariance-equipped emergent pool still wins.}
    \label{fig:replay}
\end{figure}

\textbf{Replay does not rescue the monolith (pre-registered D1).} The
cheapest continual-learning remedy short of a pool is a monolith whose
episode buffers are \emph{decoupled} from head eviction (the data
survives when the parameters are evicted) plus a generous full
burst-refit on the retained buffer at every head recreation---an
implementation committed in advance (\texttt{PREREG\_FRENCH49.md}~D) so
that the deficit could not survive as an artifact of our own SGD
budget. It closes only $56.3\%$ of the A1$-$A9 deficit (A1r $0.0329$;
A1r$-$A9 $= +0.00073 \pm 0.00025$ paired; paired-$t$
$p{=}1.4{\times}10^{-7}$, unpaired Welch $p{=}0.011$)---\emph{below}
our lodged $60\%$ prediction, which activates the registered
anti-branch: eviction-coupling is \emph{stronger} than the theory
requires, i.e., the deficit is not merely a data-loss effect. And
replay remains far above A6 ($p{=}7.0\times10^{-14}$). Equipping
replay with the invariance objective helps (A1r$^{+}$ $0.0314$,
beating the ERM-trained oracle assignment A9 at Holm $0.023$) but
still loses to both invariant pool arms (vs.\ A6 Holm $0.040$; vs.\
A10 Holm $0.017$): retention bought in data space at $O(\text{buffer})$
memory is worth less than retention bought in parameter space, and the
assignment---not just the storage---carries value.

\textbf{A fourth, unregistered outcome: reward-coupled serving alone
is anti-selective (pre-registered D4).} The organizational-carrier
probe A3$'$ (A3's reward-driven capital and serving kept verbatim,
training made reward-independent: each specialist trains its own
regime regardless of capital share) chose a fourth outcome the
registration did not lodge, reported at full prominence: A3$'$ is
strictly \emph{worse} than A3 ($0.04580 \pm 0.00040$ vs.\
$0.03748 \pm 0.00044$ post-reactivation, 100 seeds; Holm
$1.7\times10^{-68}$; training intensity matched, serving path
inherited verbatim). The mechanism, identifiable in the served-head
telemetry, is that lagged capital routes serving to the previous
regime's owner---which under siloed training holds nothing for the
active regime (the capital leader holds an active-regime head on
$39.7\%$ of days against A3's $94.6\%$)---so keeping dormant desks'
research alive does not help if capital still routes the serving
decision.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig13_emergent.pdf}
    \caption{Emergent interference (E1m). Left: under a shared MLP trunk
    with nothing evicted, the A1$-$A9 gap is $\approx 4\times$ larger in
    post-reactivation probe windows than at steady state --- a forgetting
    transient, measured rather than injected. Right: the deficit grows
    with SNR under gradient descent, diverging from the linear E6
    inversion (unpaired CIs for the linear series); the single-readout
    caveat is discussed in the text.}
    \label{fig:emergent}
\end{figure}

\subsection{E1m --- emergent interference under gradient descent}
\label{sec:e1m}

The controlled results above use an explicit memory model (LRU slots
or interference decay), inviting the objection that the forgetting is
\emph{coded, not emergent}. The pre-registered E-X4 battery
(\texttt{PREREG\_FRENCH49.md}~E) removes the model: a shared trunk
($20 \to 32 \to 32$, tanh) with linear heads on the shared
representation, the \emph{same} architecture for every arm, plain SGD
on the same SPO+ surrogate, and nothing is ever evicted---the monolith
is one trunk and one readout, A9 pins per-regime heads sharing the
trunk, and A5/A6 run the batched competition over trunk-sharing
specialists. Forgetting can arise only as representation interference.
It does: $\Gamma_{\mathrm{mlp}} = +0.01009 \pm 0.00037$ (A1$-$A9
paired, 100 seeds, $p = 1.9\times10^{-78}$), and it is
reactivation-localized---the probe-window gap is ${\approx}4\times$
the steady-state gap---interference \emph{measured}, not injected.
The A1-vs-A9 contrast is the classical single-head-versus-multi-head gap
of task-incremental continual learning \citep{vandeven2019three,
farquhar2018towards}, and interference carried by a shared
representation is well documented \citep{ramasesh2021anatomy}; what this
section adds is not the existence of trunk interference but its
\emph{allocation} reading: at fixed architecture, with no eviction
machinery and no task identity given, the gap opens under reward-driven
assignment, is reactivation-localized in decision regret, and is closed
by emergent competition that matches the oracle it was never handed.
The paired steady-state gap ($+0.0026$) reflects the single readout's
conditioning-capacity disadvantage; the reactivation-localized
\emph{excess}---$+0.0075 \pm 0.0004$, the probe gap net of the steady
gap---is the emergent-forgetting quantity proper.
Emergent competition again matches the oracle it was never handed
(A6 $0.0328$ vs.\ A9 $0.0329$ post-reactivation), pinning the three
common regimes in every seed with the crisis niche uniquely owned but
unpinned, as in E1. Two boundaries, at the same prominence as the
result. First, an honest null: the invariance axis is \emph{absent}
under the MLP (A6 vs.\ A5, $p = 0.13$)---we do not claim the
invariance effect beyond the linear specialists on this evidence.
Second, a lodged divergence: at $4\times$ SNR the linear E6 inversion
does \emph{not} reproduce---$\Gamma_{\mathrm{mlp}}$ grows
${\approx}5\times$ instead of vanishing---with the recorded
architectural caveat that the MLP monolith is a single readout that
every regime switch must overwrite, not the regime-keyed $K{=}2$ head
store of the linear A1, so the two monoliths are not apples-to-apples
and the E6 boundary stands as the \emph{linear} result. What this
section establishes is the narrower, load-bearing point: under vanilla
gradient descent with no eviction machinery at all, a capacity-bounded
reward-driven learner loses dormant-regime service, and a
reward-independent assignment protects it.

\subsection{E0 --- the structure diagnostic on real data, and an honest
null}
\label{sec:e0}

Before any regime-aware mechanism is credited on real data, the
diagnostic-first protocol requires evidence that regime structure
\emph{exists on the decision metric}: an oracle regime-conditioned
predictor (one ridge model per causal regime label, walk-forward refit)
must beat the same machinery under block-shuffled labels. GAUSE's oracle
audit found crypto and commodities carry no exploitable regime structure
under the \emph{prediction} metric \citep{li2026gause}; since
prediction-optimal and decision-optimal differ (the core
predict-then-optimize lesson), we pre-registered the re-test under
decision regret as a finding either way.

\begin{table}[t]
\centering
\small
\caption{E0: structure diagnostic on the decision-regret metric
(walk-forward, second-half evaluation; shuffled = block-label permutation,
10 draws). Negative gap = conditioning is (insignificantly) \emph{worse}
than shuffled. No domain--labeler pair passes; the real-data dissociation
is therefore predicted null, and is (Section~\ref{sec:e1s}).}
\label{tab:e0}
\begin{tabular}{llcccc}
\toprule
\textbf{Domain} & \textbf{Labeler} & \textbf{Real} & \textbf{Shuffled} &
\textbf{Gap} & \textbf{$z$} \\
\midrule
Crypto (5 pairs, daily)      & L1 vol$\times$trend & $0.01220$ & $0.01202$ & $-1.5\%$ & $-0.66$ \\
Crypto (5 pairs, daily)      & L2 filtered HMM     & $0.01212$ & $0.01204$ & $-0.7\%$ & $-0.17$ \\
Commodities (3, daily)       & L1 vol$\times$trend & $0.00834$ & $0.00832$ & $-0.3\%$ & $-0.14$ \\
Commodities (3, daily)       & L2 filtered HMM     & $0.00836$ & $0.00838$ & $+0.2\%$ & $+0.16$ \\
\bottomrule
\end{tabular}
\end{table}

The answer (Table~\ref{tab:e0}) is unambiguous: \textbf{no exploitable
regime structure on the decision metric} in either domain, under either
causal labeler---regime-conditioned regret is statistically identical to
block-shuffled-label regret (all $|z| < 0.7$), and both are identical to a
pooled unconditioned model. The prediction-metric null of GAUSE
\emph{extends} to the decision metric here: the PnO distinction, which in
principle could resurrect structure invisible to prediction error, does
not do so on these panels. We bound the claim honestly: these are small
universes (five crypto pairs, three commodities) at daily frequency with a
standard momentum/volatility feature inventory and two labelers; richer
features, intraday horizons, or larger cross-sections could yet pass the
gate. What the protocol forbids is claiming the mechanism's benefits where
the gate fails---which is exactly what a less guarded evaluation would
have produced, since the machinery happily trains and ``specializes''
on structureless labels (the GAUSE negative control: organization is not
structure).

\paragraph{The intraday retest: the gate can pass.} The bound above is
not vacuous hedging: re-running the identical protocol at intraday
frequency with a richer causal feature inventory (ten features from full
OHLCV bars---range-based volatility, volume $z$-score, reversal,
multi-horizon and cross-sectionally ranked momentum) locates a substrate
where the gate \emph{passes}. Across a pre-registered
$\{1\mathrm{D}, 4\mathrm{H}, 1\mathrm{H}\} \times \{\mathrm{L1},
\mathrm{L2}\} \times \{\text{bar-native}, \text{wall-clock}\}$ screen
(ten cells): richer features alone do \emph{not} rescue the daily gate
($|z| < 1.1$)---the binding constraint is the episode inventory, not the
features---and every wall-clock-window cell fails, while two bar-native
cells (whose faster label dynamics multiply episode counts roughly
sevenfold) pass the screen. A pre-registered confirmation with 50 fresh
shuffle controls and a split-half stability requirement confirms one:
\textbf{4-hour crypto under the bar-native L2 labeler} (951 episodes;
conditioning beats shuffled labels by $3.15\%$, $z = 2.32$, both halves
positive at $+3.4\%/+2.9\%$); the 1-hour cell fails confirmation
($z = 1.76$) and is reported as screen-only. This is the first real-data
pass of the structure diagnostic on the decision metric in this line of
work, and we state its selection caveats at the same prominence as the
pass: it is one confirmed cell from a ten-cell screen (fourteen
real-vs-shuffled tests counting the daily battery), the confirmation
reuses the same data span as the screen, and the selected cell's gap
estimate is biased upward by selection on the maximum. A
disjoint-period replication was the committed next test before any
claim is built on this substrate; the attempt is on record as
unexecutable---the disjoint period's data proved unfetchable (the
exchange archive is geo-blocked and tick-only)---so the commitment
stands unmet rather than quietly dropped, and no claim is built on
this substrate. The pass sets up the sharper question
of Section~\ref{sec:e1r}: does a passing gate suffice for the mechanism
to pay?

\subsection{E1s --- the real-data dissociation is null, as the failed gate
predicts}
\label{sec:e1s}

We ran the ten-arm comparison (the arms of Table~\ref{tab:e1} except the
A10 oracle-INV skyline and the later-added A1r replay pair) on
\emph{regime-stitched} real crypto data:
contiguous same-label day-blocks of the actual cross-sections
(real returns, real features), re-arranged into controlled schedules with
the rare regime dormant $\approx 200$ days (100 seeds). No pair survives
Holm correction in the pre-registered test family (min Holm $p = 0.17$;
post-reactivation regret $0.0129$--$0.0136$ across arms); the one raw
trend---A6 below A1 at uncorrected $p = 0.028$---we disclose but do not
claim, and the family-level conclusion remains null under the
pre-registered Holm-corrected test. The numerically lowest arm is
A8a---Hedge
over \emph{fixed, non-learning} strategies, at $0.0129$---which
sharpens the null rather than softening it: where the gate fails, a
baseline that learns nothing edges out every learning arm, ours
included. Under a failed E0 gate
this is the \emph{predicted} outcome---there is no per-regime structure
for a specialist to retain, so retention cannot pay---and we present it as
the protocol working, not as the mechanism failing: the same arms, the
same code, and the same statistics that produce the sharp dissociation of
Table~\ref{tab:e1} produce a flat table when the precondition is absent.
A method whose evaluation cannot go flat on structureless data should not
be trusted when it goes sharp. (Fallback F1 of the pre-registered design:
where the gate fails on all available real domains, the headline is the
controlled study plus the diagnostic protocol; the equities panel, with
its richer episode inventory, is the planned real-data test.)

\subsection{E1r --- dissociation under a \emph{passed} gate: the second
gate}
\label{sec:e1r}

The confirmed intraday pass reverses the falsifiable prediction: with
decision-metric structure present, the pre-registered ordering
$\text{A6} < \text{A5} \ll \text{A1}$ \emph{should} emerge. We ran the
ten-arm comparison on regime-stitched 4-hour crypto under exactly the
confirmed cell (bar-native L2 labels, the rich feature inventory, the
rare vol-up regime dormant $\ge 80$ bars, probe 15 bars, 20 seeds). The
result is a second null. All pre-registered pairs are flat
($p > 0.57$ throughout; A6 $0.0058 \pm 0.0002$ vs.\ A1
$0.0059 \pm 0.0002$). We state the record plainly: the pre-registered
prediction for this experiment was separation, and it was \emph{refuted}.
Two features of the table localize the cause.
First, \emph{learning now pays}: A8a (Hedge over fixed, non-learning
strategies), the numerically best arm on the structureless daily
substrate of Section~\ref{sec:e1s}, is here the \emph{worst}
($0.0061$)---consistent with the passed gate. Second, the
post-reactivation elevation ($\approx{+}7\%$ over each arm's own overall
regret) is carried \emph{equally by A9, the hand-pinned oracle that
cannot forget by construction}: the measured forgetting deficit
$\Gforget = \text{A1} - \text{A9}$ (post-reactivation) is $+0.000002$,
zero to four decimals, against $+0.0017$ in the controlled markets of
Table~\ref{tab:e1}. Reactivations are hard here because regime onsets
are intrinsically hard, not because anything was forgotten: even with
dormancy imposed at $\ge 80$ bars by the stitched schedule, a fresh
head refits the regime within the 15-bar probe at this bar density and
signal level---the E6 boundary (Section~\ref{sec:e6}) observed in the
wild rather than swept in simulation. Structure exists
(gate 1), but there is nothing for retention to harvest (gate 2)---the
$\Gforget$ term of Proposition~\ref{prop:decomp}, measured here at
zero, explains \emph{retrospectively} why no allocation policy
separates. We are explicit about the epistemic status: this is a
diagnosis made after the refutation, not a prediction that preceded
it---the decomposition supplied the vocabulary, and the honest test of
the diagnosis is out-of-sample. That test is the upgraded protocol
itself, stated as a falsifiable commitment for every substrate we touch
next: \emph{two gates}---structure on the decision metric, then a
positive forgetting deficit via the A1-vs-A9 counterfactual, both
cheap, both causal, both run before any mechanism is bought---with the
pre-registered prediction that the dissociation emerges if and only if
both pass. Real data now instantiates three cells of the
precondition matrix (no-structure $\Rightarrow$ null, predicted in
advance; structure without harvestable forgetting $\Rightarrow$ null,
diagnosed after the fact; the controlled study's
structure-with-forgetting $\Rightarrow$ sharp dissociation); the
equities cross-section, whose crisis regimes recur on multi-year scales
at daily frequency, targets the fourth---and is the first substrate on
which the two-gate prediction is exposed to refutation. That exposure
happened (Section~\ref{sec:e1f}), and the protocol did not survive it
intact.

\subsection{E1f --- daily equities (49 industries): the deficit exists,
and its sign prices the pool}
\label{sec:e1f}

We took the two-gate commitment to the canonical equity cross-section:
the Ken French 49 industry portfolios, daily, value-weighted, 1990--2025
(9{,}067 days, all 49 industries complete; survivorship handled by
construction), built from the 2026-05 CRSP database. Design and
predictions were pre-registered before each run
(\texttt{PREREG\_FRENCH49.md}): the paper's exact gate and dissociation
protocols, 50 shuffle controls from the start, $k{=}5$/$w_{\max}{=}0.2$
for the wide cross-section, an eight-feature return-based causal inventory,
and a labeler family disclosed in full---L1/L2 (the paper's vol-band and
filtered-vol labelers; two cells, pre-registered together) and L3 (an
index-drawdown $\times$ trend labeler with genuinely slow crisis
dynamics, designed \emph{after} seeing the L1/L2 gate results and
pre-registered separately before any L3 statistic was computed;
family-wise accounting is over all three cells). In revision, further
batteries were added, each lodged in
\texttt{PREREG\_FRENCH49.md} before its runs: mechanism probes
(X1/X3), transaction costs (D3), a replay baseline (D2), 100-seed
reruns of the main batteries (D6), a withheld-era replication on
1926--1989 (E-F), probe-window/dormancy and decision-layer
robustness grids (D8, K3), event-level inference on the
per-reactivation deficits (K1), an expanding-window/replay battery
at the full seed budget (K2), and a first non-US regional cell
(Japan, Addendum L). This section reports the whole record.

\paragraph{Gates.} We report gate statistics in the permutation
register: $z$ against the 50-control shuffle distribution, whose
empirical $p$ cannot fall below $1/51 \approx 0.02$; a normal fit to the
control distribution gives the indicative $p$ we quote. (The
pre-registration's auxiliary $t$-based sub-criterion tests against the
shuffle \emph{mean} with an SE denominator; it is register-inflated by
$\approx\sqrt{50}$ and non-discriminating---L2 passes it while failing
$z$---so the binding criteria are $z$, the pooled comparison, and
split-half stability, as registered.) L1 passes gate 1
cleanly---conditioning beats the shuffle controls by $1.14\%$
($z{=}3.18$, empirical $p \le 1/51$, normal-fit
$p \approx 7\times10^{-4}$, both halves positive, conditioned beats
pooled)---the first \emph{daily-frequency} structure pass in this
program, with no screen-selection caveat (the family is three disclosed
cells). L2 fails ($z{=}0.88$). L3 presents the instructive case: it
clears the shuffle criterion ($+0.87\%$, $z{=}2.24$, normal-fit
$p \approx 1.2\times10^{-2}$, halves $+0.86\%/+0.89\%$) but fails the
pooled sub-criterion by a hair (pooled beats conditioning by $0.10\%$),
so by the pre-registered letter it \emph{fails} gate 1. Gate 2, however,
inverts the pattern: on the L1 walk-forward the measured deficit is
zero-to-negative ($\hat\Gamma_{\mathrm{forget}} = -0.00013 \pm 0.00007$
at 100 seeds---the interval now excludes zero from below),
while on the L3 walk-forward it is \textbf{positive for the first time
on any real substrate in this program}:
$\hat\Gamma_{\mathrm{forget}} = +0.00089 \pm 0.00011$ (100 seeds,
reproducing the 20-seed registration battery; the $\pm$ is
implementation precision only---seed noise conditional on one
history; see caveat (i) and the event-level register below). One
labeler-anatomy fact matters for interpretation and we state it
precisely: the \emph{union} of L3's crisis regimes is not dormant for
long stretches---the 2010, 2011, 2015--16, and 2018 near-miss episodes
are all in the crisis inventory, recurring every one to three
years---while the specific crisis-\emph{with-uptrend} cell sleeps up to
$2{,}953$ trading days. The retained specialists therefore do get
occasional live-fire recalibration on near-misses between major crises;
the deficit is measured net of those rehearsals, which is the
economically relevant quantity.

\paragraph{The threshold sweep.} The pre-registered threshold-robustness
sweep (\texttt{PREREG\_FRENCH49.md} C) then delivered the section's
hardest result, reported here at the same prominence as the positive:
\textbf{the deficit exists only at the pre-registered $15\%$ cutoff}.
At $10\%$ and $12\%$ (denser crisis inventory, shorter dormancies)
$\hat\Gamma$ is null ($+0.00013 \pm 0.00027$, $-0.00010 \pm 0.00017$)
and the ordering dissolves; at $20\%$ (sparser inventory)
$\hat\Gamma = -0.00045 \pm 0.00026$ and the monolith again beats A6.
The $15\%$ value was fixed in the pre-registration before any L3 run,
so this is not a tuned peak---but it is a \emph{fragile} one, and the
sweep refutes our pre-registered robustness prediction (P8). Two
readings, both stated: the fragility is an artifact warning (the
practitioner's reading---the effect lives in one labeler
specification on one history), or it is a granularity window exactly
parallel to Proposition~\ref{prop:breakeven}'s dormancy window---too
fine a threshold and near-miss rehearsals keep every learner fresh
($\hat\Gamma \to 0$, as the E6 axis predicts), too coarse and the
qualifying reactivations become too few to harvest within one history
(premium dominates)---with $15\%$ landing, by pre-registered luck or
by economics, inside the window. The second reading was post-hoc when
first written; the withheld era below was its first discriminating
test, and neither simple reading survived it intact. (Both
statements in this paragraph are the coarse-grid, zero-hysteresis
record; the lodged fine threshold map has since adjudicated the
window-vs-island question---\emph{window}, on both eras---and is
reported with the withheld era below.)

\paragraph{Measurement and decision-layer robustness (D8, K3).} The
threshold is the fragile axis; the measurement and decision axes are
not. Across the registered probe-window $\times$ dormancy-threshold
grid (probe $\{5, 10, 15, 30\}$ days at dormancy 90; dormancy
$\{60, 90, 120\}$ at probe 15; shared trajectories, measurement-only
axes), $\hat\Gamma$ is positive-significant in every cell---from
$+0.00134 \pm 0.00054$ at probe 5 to $+0.00057 \pm 0.00011$ at probe
30, and $+0.00103 \pm 0.00014$ / $+0.001165 \pm 0.000323$ at dormancy
60/120. Perturbing the decision layer itself at constant gross
exposure ($k \cdot w_{\max} = 1$, K3) preserves both the sign and
the A6-vs-A1 direction in both lodged cells: $k{=}10$/$w_{\max}{=}0.1$
gives $\hat\Gamma = +0.000495 \pm 0.000123$ with A6 beating A1 at Holm
$3.5\times10^{-13}$; $k{=}3$/$w_{\max}{=}1/3$ gives
$\hat\Gamma = +0.00027 \pm 0.00024$---positive-significant with a
thin margin, CI lower edge $+0.00002$, disclosed---with A1$-$A6
$= +0.00058 \pm 0.00017$ paired (Holm $1.4\times10^{-4}$). No
decision-layer fragility branch triggered. One aggregated disclosure
is stated once rather than scattered: across all three swept axes the
measured \emph{magnitude} peaks at the registered specification
($k{=}5$ among $\{3,5,10\}$; the $15\%$ drawdown threshold among its
sweep; and the probe profile's knee at the registered 15 days)---a
selection-adjacent pattern we disclose rather than bury; the
registered predictions and the sign rule consume only the sign,
which is stable everywhere measured on these axes.

\paragraph{Dissociations at 100 seeds.} The ten-arm
walk-forward comparison ran on both labelers (registered at 20 seeds,
re-run at 100 under the pre-registered parity battery D6 with outcomes
unchanged; 100-seed values quoted throughout), dormancy
threshold 90 days. Under L1 ($\hat\Gamma < 0$): a significant
\emph{inversion}---the monolith beats \nm{}-full (A1 $0.01995$ vs.\ A6
$0.02026$, Holm $1.2\times10^{-26}$), the oracle-pinned retainer beats A6
(Holm $8.0\times10^{-7}$)---the invariance premium paid with no
insurance claim to collect, the steady-state tax of
Section~\ref{sec:e1} surfacing in the probe window itself; on
\emph{overall} regret the two are within a hair
($0.01849$ vs.\ $0.01848$), so the L1 inversion is a probe-window
transient, not a net loss---and the costs paragraph below shows it is
also a \emph{gross-only} phenomenon. Under L3 ($\hat\Gamma > 0$): the
\textbf{full pre-registered ordering emerges}---A6 $0.02128 \pm 0.00004$
$<$ A5 $0.02165$ $<$ A1 $0.02252$, with the capacity-clean retention
contrast (A5 vs.\ the total-capacity-matched router A2) at Holm
$1.2\times10^{-20}$, the monolith contrast at Holm $1.2\times10^{-40}$
(impure in total capacity, as in Section~\ref{sec:e1}; no capacity
sweep exists on this substrate yet), the invariance axis at Holm
$1.6\times10^{-15}$ (A6 vs.\ A5), the joint effect at Holm
$5.2\times10^{-65}$ ($-5.5\%$), and A6 beating the hand-pinned oracle
(Holm $1.5\times10^{-12}$)---the synthetic study's signature at one-half
the synthetic effect size, and this time the pool arms also lead on
\emph{overall} regret (A6 $0.01847$, A5 $0.01846$ vs.\ A1 $0.01858$):
the probe gain is not repaid in steady state here. One new wrinkle is
disclosed rather than absorbed: at 100 seeds the \emph{stitched}
counterparts, previously flat in $\hat\Gamma$, turn marginally
positive under both labelers (L3 $+0.00023 \pm 0.00016$; L1
$+0.00023 \pm 0.00015$) while their arm families stay flat (min raw
$p = 0.50$ and $0.34$)---the schedule-resampling design now leans the
deficit's way without any arm separating, a favorable drift we flag
precisely because it was not predicted.

\paragraph{Event-level inference (K1, N1): the register in which
most cells weaken.} Seed-level CIs on a walk-forward measure
Monte-Carlo precision of the pipeline, not generality across crises,
so the K1 and N1 addenda registered event-level tests on the
per-reactivation deficits $\gamma_i$ of all four positive
walk-forward cells (one-sided sign test and one-sided Wilcoxon,
$\alpha{=}0.05$ per test per cell; the event-level $t$ is
descriptive only, since events cluster by era). The program's
event-level scoreboard, stated once and in full: the \textbf{NBER
cell is the only cell that passes both registered tests}
($n{=}20$; sign $p = 0.021$, 15 of 20 positive; Wilcoxon
$p = 0.016$)---notable because its LORO concentration profile was
the weaker one, and its pass was lodged in advance at deliberately
low confidence. The L3 cell ($n{=}27$, median $+4.5$ bps) and the
withheld-era $10\%$ cell ($n{=}56$, median $+1.5$ bps) pass the
Wilcoxon only (Wilcoxon $p = 0.016$ and $p = 0.029$; sign tests
$p = 0.061$ with 18 of 27 positive---19 were needed---and
$p = 0.175$ with 32 of 56: the mean is carried by large positive
events while the median is weak). The 1958--89 cell \emph{fails
both} registered tests ($n{=}21$; sign $p = 0.332$, Wilcoxon
$p = 0.393$), the weakest event-level showing in the program. The
lodged adverse branches fire and are taken in full: the
seed-register CIs of the L3 headline \emph{and of both withheld-era
positive cells} are labeled \textbf{implementation-precision-only}
wherever quoted (abstract included), and the L3 cell carries its
three travel-together caveats (threshold fragility, single history,
event-level sign-test miss)
(\texttt{results/e\_event\_level\_inference.json},
\texttt{results/e\_prewar\_event\_level.json}).

The capstone is the \emph{pooled} cross-history register (Addendum
O), lodged with independent histories counted once---US-modern
L3@$15\%$ ($n{=}27$), US-prewar $10\%$ ($n{=}56$), and Japan $15\%$
($n{=}26$, its events pooled \emph{with their signs}; NBER and
1958--89 excluded as overlapping the same histories)---and its three
registers split. The pooled sign test \textbf{fails} (63 of 109
positive, $p = 0.0625$), so the lodged adverse branch fires
\emph{program-wide}: the frequency form of the deficit claim
(``the typical reactivation pays'') is retired---no cell cites event
frequency \emph{as support for the deficit claim}; per-cell
registered test outcomes, including NBER's sign-test pass, continue
to be reported as their lodged verdicts. The pooled Wilcoxon
\textbf{passes} ($p = 0.0086$; median $+1.4$ bps, mean $+2.6$
bps)---the magnitude register survives a pool deliberately hostile
to it. And the dollar-weighted register, lodged as economically
primary (a desk collects $\gamma_i \times N_p$ per event, not a
vote count), sums to $+4{,}274$ bps-days with a bootstrap 95\% CI
of $[-1{,}881, +9{,}977]$ covering zero---so, per the lodged
consequence, the program's economic headline is restated as
\textbf{region-conditional}, not pooled-positive: US-modern
$+4{,}859$, US-prewar $+1{,}542$, Japan $-2{,}127$ bps-days, a
difference of large opposite-signed regional bets. One distinction
must not be blurred, because the two objects share a word: the
pooled \emph{sign test} is a frequency claim about individual
reactivations, and it is retired; the \emph{sign rule} is a
cell-level pricing diagnostic scored in both directions, and it
stands at 9 of 10 out-of-sample---indeed the pooled dollar
decomposition above is exactly the structure the sign rule prices
and a pooled register cannot
(\texttt{results/e\_pooled\_event\_level.json}).

\paragraph{Mechanism honesty: the deficit is not (yet) forgetting.}
Two registered probes (E-X1, E-X3) asked \emph{why} the L3 deficit
exists, and both returned the answer we did not want. The X1
regression of per-reactivation deficits $\Gamma_i$ on log
cell-dormancy, log union-dormancy, and rehearsal count---with event
fixed effects across thresholds, exploiting the same calendar crises
appearing at multiple thresholds with different rehearsal
histories---finds \emph{no} explanatory covariate (all registered
slopes n.s., min $p = 0.44$ pooled, within-event likewise; probe-overlap
dilution ruled out at $\le 4.4\%$). The X3 half-life probe is sharper:
at the $15\%$ threshold the A1$-$A9 gap does \emph{not} decay across
the 15-day probe window (tail $\ge$ start; exponential fits
$R^2 < 0.2$)---a \emph{level offset}, not a relearning transient;
where a true transient exists ($20\%$), it relearns in 2--4 days and
nets to zero, exactly the E6 mechanism. We therefore execute the
rename the X3 registration committed us to: on real data the measured
quantity is a \textbf{regime-conditional allocation deficit}---the
capacity-bounded, reward-driven learner serves crisis windows
persistently worse---and \emph{eviction-forgetting} remains
demonstrated only in the controlled settings (the linear E1--E6 and
the MLP of Section~\ref{sec:e1m}). The symbol $\hat\Gamma$ and its
definition (A1$-$A9) are unchanged, as is the deployment consequence
(the counterfactual prices the pool either way); what changes is the
mechanism attribution, and we carry the corrected name through the
real-data claims below.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig11_costs.pdf}
    \caption{Crisis-window transaction costs, both-pay convention. Left:
    the L3 deficit $\hat\Gamma$ strengthens monotonically with the cost
    tier (gross point at 100 seeds; net tiers at 20 seeds, annotated).
    Right: the L1 walk-forward inversion reverses net of $25$ bps ---
    the inversion, not the deficit, was the gross-only phenomenon.}
    \label{fig:costs}
\end{figure}

\paragraph{Costs on 1990--2025: the deficit survives and
strengthens---era scope stated up front.} Everything in this
paragraph is the 1990--2025 record; the withheld era's net register
is reported with the withheld era below, where the amplification
clause \emph{fails} out-of-sample. The
pre-registered cost battery (D3; both-pay convention---arm and oracle
each charged their own turnover---with the arm-only variant as lodged
sensitivity) charges crisis-window effective costs of 25/50/100 bps.
The net-of-cost deficit \emph{strengthens monotonically with the cost
tier}: $\hat\Gamma_{\mathrm{net}} = +0.00126 \pm 0.00020 \to +0.00161
\pm 0.00021 \to +0.00230 \pm 0.00024$, with the full four-arm ordering
(A6 $<$ A9 $<$ A5 $<$ A1) intact at every tier
(A6-vs-A1 Holm $4.6\times10^{-19}$ at 25 bps, strengthening to
$1.3\times10^{-29}$ at 100 bps). The mechanism is the registered one: a
reactivated monolith churns its top-5 book hardest exactly when spreads
are widest (A1 turnover $1.03$/day vs.\ A6 $0.62$). The lodged
arm-only-pays sensitivity is not merely consistent but \emph{identical}:
orderings, $\hat\Gamma_{\mathrm{net}}$ values, and $p$-values match the
both-pay convention at every tier, because the oracle's cost term
cancels in the regret difference. Our own lodged
pessimism---``the overall-regret margin is expected \emph{not} to
survive cost adjustment''---is refuted in the favorable direction. And
the L1 inversion \textbf{reverses net of costs}: A6 beats A1 at every
tier (paired $-0.00075 \pm 0.00010$ at 25 bps, $p = 1.2\times10^{-16}$)
---the inversion, not the deficit, was the gross-only phenomenon. One
cost slice is reported as sensitivity, not re-adjudication: charging
costs inside the gate-1 comparison flips L3's pooled sub-criterion
($-0.10\%$ gross $\to$ $+3.0\%$ net); the gate as registered is a
gross-metric criterion and remains a FAIL, and we do not re-open it.
The practitioner countermeasure these numbers invite was itself
pre-registered (\texttt{PREREG\_FRENCH49.md}~H) and run: a
\emph{trade-banded} monolith,
identical to A1 except that the served book updates only when more
than $b$ names would change, $b \in \{1, 2\}$
(\texttt{results/e\_french49\_banded.json}). Banding is free---gross
regret is unharmed and net regret improves ($b{=}2$ vs.\ A1 net Holm
$1.3\times10^{-3}$ on L3)---yet it cannot buy the pool's advantage:
A6 beats both banded monoliths net of 25 bps on L3 (Holm
$\le 7.1\times10^{-17}$; the A1$-$A6 net gap remains $97\%$/$79\%$
intact at $b{=}1$/$b{=}2$) and on L1 (residual
$+0.00028 \pm 0.00011$, Holm $2.1\times10^{-5}$)---with the honest
disclosure that $b{=}2$ closes $62\%$ of the L1 net-reversal
magnitude, so the reversal sentence above keeps its label with the
attenuation reported alongside. The lodged magnitude clause
\emph{failed} and is disclosed rather than renarrated: the registered
bands cut A1's turnover only $4.1\%$/$15.7\%$ (${\ge}30\%$ was
lodged), because A1's crisis churn comes from daily re-rankings of
more than two names, which defeat narrow bands. The conclusion the
battery was built to force is the one it delivers: \emph{selection
quality, not just churn, carries the deficit}.

\paragraph{Replay and expanding windows: data retention versus
parameter retention.} The decoupled-replay monolith of
Section~\ref{sec:e1} was run on L3 (D2, 20 seeds), then re-measured
at the full 100-seed budget alongside an \emph{expanding-window}
monolith A1e (K2,
\texttt{results/e\_french49\_L3\_expwin\_100s.json})---a retrainer
whose fit consumes the entire history to date, so no data is ever
discarded; it sits outside A1's class, whose bounded capacity forces
recency. The 100-seed register supersedes the 20-seed pilot
throughout this paragraph (one reversal below); the pilot is
retained in the custody ledger. Replay collects essentially the
entire deficit---the residual over the replay monolith is
$+0.00004 \pm 0.00012$, n.s., ${\approx}96\%$ closure against
$56\%$ in the controlled market---so on this substrate \emph{data}
retention matches \emph{parameter} retention against the ERM oracle,
consistent with the allocation-deficit reading above. The
expanding-window desk does worse: A1e collects $58.7\%$ of the
deficit, and its residual is \emph{real}---A1e$-$A9
$= +0.000366 \pm 0.000128$, positive-significant, ${\approx}3.7$
bps/day on the post-reactivation register (the 20-seed pilot's null
residual reversed at 100 seeds)---and A1e loses both to replay
(Holm $4.4\times10^{-6}$) and to the pool (A1e$-$A6
$= +0.00072 \pm 0.00011$ paired, Holm $1.4\times10^{-23}$). The
registered relabeling branch does not trigger: A6 still beats
replay---paired A1r$-$A6 $= +0.000389 \pm 0.000096$, paired
$p = 2.7\times10^{-12}$ (Welch $2.9\times10^{-14}$), computed
post-hoc from the K2 battery's released raw seeds because the lodged
Holm family omitted exactly this pair, and superseding reliance on
the 20-seed Holm $0.013$ previously carried here---and beats
replay+INV (Holm $4.5\times10^{-4}$; 20 seeds, the only register in
which that arm exists). The pool-superiority claim survives its most
dangerous baseline, and the reason is informative: what neither a
buffer nor an ever-growing training window buys is the
invariance-equipped emergent assignment.

\begin{figure}[t]
    \centering
    \includegraphics[width=0.85\textwidth]{figures/fig14_window.pdf}
    \caption{The zero-hysteresis coarse sweep: $\hat\Gamma$ versus
    drawdown threshold for 1990--2025 (peak at the pre-registered
    $15\%$) and the withheld 1926--1989 era (peak at $10\%$). At this
    register the window appears to relocate with crisis density;
    the lodged fine map (E-X2, text) adjudicates \emph{window} on
    both eras with an overlapping $13$--$16\%$ band under exit
    hysteresis, superseding the relocation reading---what this
    figure shows is the $h{=}0$ slice.}
    \label{fig:window}
\end{figure}

\paragraph{The withheld era: 1926--1989.} Everything above shares one
36-year history. The registered E-F battery replicated the
byte-identical L3 protocol---frozen $15\%$ specification, same
features, arms, tests, and the lodged sign hypothesis; zero new
researcher degrees of freedom---on an era the design had never seen:
the 36 industries with complete daily history, 1926-07-01 through
1989-12-31 (16{,}983 days, Depression through the 1987 crash). The
design follows the pre-discovery-sample tradition of
\citet{linnainmaa2018history}, whose base rate is the right prior
here: most in-sample anomalies attenuate or die pre-sample, and
generic pre-sample decay predicts attenuation \emph{everywhere}---not
a sign record that scores in both directions with an era-stable
paying band; the out-of-sample discipline is
the Goyal--Welch standard \citep{goyal2008comprehensive,
mclean2016does}. Gate 1
delivers its strongest pass in the program ($z = 23.5$; conditioning
beats pooled by $+10.4\%$)---and the frozen $15\%$ cell
\emph{inverts} ($\hat\Gamma = -0.00024 \pm 0.00008$; A1 beats A6,
Holm $2.1\times10^{-3}$): the strongest structure signal we have ever
measured coexists with a pool that prices \emph{negatively}, which is
what finally demotes gate 1 to a structure screen. The sign rule, by
contrast, scored. Six withheld cells were evaluated (thresholds
$10/12/15/20\%$ on the full era; 1926--57 and 1958--89 sub-eras at
$15\%$): \textbf{five of six are consistent with the lodged rule, in
both directions}, including two positive-deficit cells where the full
A6 $<$ A5 $<$ A1 ordering emerges out-of-sample---$10\%$
($\hat\Gamma = +0.00020 \pm 0.00009$, A6-vs-A1 Holm
$1.9\times10^{-6}$) and 1958--89 ($\hat\Gamma = +0.00021 \pm 0.00015$,
Holm $2.1\times10^{-4}$); both intervals are implementation
precision only, per the event-level register above (K1,
N1)---and two negative-deficit cells where the
predicted inversion appears ($15\%$ full era; 1926--57, Holm $0.023$).
The single miss is the $12\%$ cell: $\hat\Gamma$ negative-significant
in the seed-noise register ($-0.00010 \pm 0.00007$) with a flat arm
table---a hit under the weak scoring (``dissociation
\emph{only if} $\hat\Gamma$ significantly positive''; the weak form is
one-directional) and a miss under the strong
directional scoring (negative $\Rightarrow$ inversion); we report both
scorings rather than choose the flattering one.

\textbf{Scorecard accounting, stated against ourselves.} The
discount comes first, because it governs everything the arithmetic
below can mean: the scorecard's evidential force is closer to
\emph{two-to-three effectively independent observations}, with both
directions represented, than to its nominal cell count---the cells
share histories and crises, the thresholds overlap, and the record's
first genuinely temporal-custody test is the lodged forward
commitment, not anything in this table. (That commitment's first
cell has since resolved as a both-forms hit; see ``The first
temporal-custody cell'' below.) With the discount in front, three
qualifications bound what ``five of six'' can mean. First, the cells
are not independent: the $15\%$ full-era cell is the \emph{union} of
the two sub-era cells---its inversion largely restates the 1926--57
inversion---and the four threshold cells share one history's crises.
Alternative countings a reader may prefer: $3/4$ (thresholds only),
$4/5$ (disjoint cells), $5/6$ (as published, conservatively scored),
$6/6$ (weak form). Second, the weak/strong scoring taxonomy was
formalized in the verdict addendum \emph{after} these runs: the
registration lodged the sign hypothesis but not the hit/miss boundary
for a negative-significant $\hat\Gamma$ with a flat table. Third, the
lodged rule was stated for walk-forward designs, and we decline to
hide behind that scoping: at 100 seeds the two \emph{stitched} designs
now show positive-significant $\hat\Gamma$ ($+0.00023 \pm 0.00015$ and
$+0.00023 \pm 0.00016$) with flat arm families (min raw $p \ge 0.34$)
---strong-form misses if scored. Counted inclusively the record is
$5/8$ under the strong form and $8/8$ under the weak form (the weak
form being one-directional, a positive-significant $\hat\Gamma$ with a
flat table is a strong-form miss, not a weak-form violation); folding in
the four NBER cells of the temporal-custody paragraph below, $8/12$
strong and $12/12$ weak (with the primary walk-forward's strong-form
status register-dependent, as confessed there); and folding in the
four Japan cells of the regional battery below (all four consistent
under both forms, per the addendum's own count), $12/16$ strong and
$16/16$ weak, with the out-of-sample-only register at $9/10$ (five
of six withheld cells plus four of four Japan cells). The sweep pattern on the coarse
grid was equally instructive at the time: the fragility of the
1990--2025 result
\emph{replicates in pattern}---the deficit again lives at essentially
one threshold, flanked by flat and inverted neighbors---but the window
appeared to \emph{relocate} ($15\% \to 10\%$) as the era's crisis
density shifts
(at $15\%$ the pre-war era spends $41\%$ of its days in the crisis
union, against $13\%$ post-1990). Both simple readings of the original
fragility are thereby refuted as stated: the fixed-threshold
\emph{artifact} reading is confirmed at the letter for
$15\%$-off-sample yet fails as a full account---the phenomenon
reappears off-sample, with the full ordering, twice---and the naive
\emph{granularity} reading (multi-year crisis-cell dormancy
$\Rightarrow$ positive deficit) fails too: multi-year dormancy is
present throughout this era and the sign still varies. What survives
both eras is the deficit's sign as the pricing diagnostic; the
window-location story itself was then adjudicated by the lodged
threshold map, which supersedes the relocation reading.

\paragraph{The lodged threshold map (E-X2): window, not island---and
the relocation narrative superseded.} The window-vs-island question
carried a lodged criterion ($\hat\Gamma$ CI $> 0$ in $\ge 3$
contiguous 1pp thresholds at some hysteresis level $=$ window), run
over the full $8$--$22\%$ $\times$ exit-hysteresis
$\{0, 2, 4, 6\,\mathrm{pp}\}$ map on both eras (tier 1 A1/A9 at 10
seeds; tier 2 four-arm at 20 where tier-1 CIs exclude zero).
\textbf{Modern era: window at every hysteresis level} (h0 and h2
$13$--$17\%$, h4 $13$--$18\%$, h6 $16$--$22\%$), with the registered
$15\%$/h0 cell inside its era's window and reproducing on tier-2
re-read ($\hat\Gamma = +0.00091 \pm 0.00020$, full ordering, min
Holm $3.3\times10^{-12}$)---the island clause is dead, and the
granularity reading is vindicated on the era that lodged it. The
negative side at equal prominence: $8\%$ is inversion territory
(h0 $-0.00040 \pm 0.00022$, Holm $4.0\times10^{-3}$; h2
$-0.00029 \pm 0.00015$, Holm $1.6\times10^{-8}$), the $12\%$/h0
wrinkle is negative-significant at tier 2 ($-0.00024 \pm 0.00022$),
and not every tier-1 edge positive survives tier-2 re-read
($20\%$/h0 $\to -0.00018$, n.s.). \textbf{Withheld era: window only
under hysteresis} (h4 $13$--$16\%$, h6 $14$--$16\%$; no $\ge 3$ run
at h0 or h2), and the lodged PX2e anchor sub-criterion
\emph{missed}, at full prominence: the era's own $10\%$ anchor sits
inside no window---an isolated positive at h0, with both 1pp
neighbors n.s.---so the prewar paying cell is threshold-isolated
exactly as the modern $15\%$ cell was feared to be, even though the
cell itself is solid on tier-2 re-read
($\hat\Gamma = +0.00013 \pm 0.00006$, full ordering, min Holm
$1.8\times10^{-8}$). The cross-era synthesis is descriptive (the
window-vs-island verdicts above are the only adjudicated claims),
and it \textbf{supersedes the relocation narrative}: with the
hysteresis dimension open, the two eras' windows \emph{overlap} at
$13$--$16\%$---the apparent $15\% \to 10\%$ relocation was an
artifact of scanning only $h{=}0$---and hysteresis \emph{flips} the
prewar $15\%$ cell from the inversion reported above
($-0.00024 \pm 0.00008$ at h0) to a positive with the full point
ordering (h4 $+0.00017 \pm 0.00008$, min Holm $1.7\times10^{-7}$;
h6 $+0.00023 \pm 0.00009$): exit-band dynamics, not the entry
threshold alone, determine which side of the sign rule a cell lands
on. Both eras keep fine-threshold ($8$--$9\%$) inversions---the
rehearsal-density side of the E6 axis---and the maps are
ordering-rich (the A6 $<$ A5 $<$ A1 point ordering holds in 23 of 38
prewar and 10 of 36 modern tier-2 cells)
(\texttt{results/e\_french49\_L3\_x2.json},
\texttt{results/e\_french49\_prewar\_L3\_x2.json}).

\paragraph{The window-location estimator ($\kappa$), retired.} One
attempted operationalization of window location is already refuted:
the window-location estimator $\theta(\mathrm{era})$ committed in the
CRSP T-split amendment---the finest grid threshold whose
crisis-union occupancy falls below a single calibration parameter
$\kappa$---failed its first calibration on the two available eras
(\texttt{results/window\_estimator\_calibration.json}): the $\kappa$
intervals the two eras require are disjoint by roughly
$3.5{\times}$ ($(0.129, 0.143]$ modern versus $(0.471, 0.486]$
pre-war) and \emph{directionally} wrong---the denser-crisis era's
h0 paying threshold moved finer, not coarser. The failure occurred
and was
disclosed before any CRSP data access; per the amendment ledger the
estimator is retired, the CRSP L3-family primary falls back to the
frozen $15\%$ threshold, and the threshold set runs as a descriptive
window-location map only. One custody note: the estimator was
committed and retired \emph{before} the hysteresis map above
existed; the map's stable cross-era band does not un-retire it, and
any successor estimator must be lodged at OSF before CRSP data
access.

\paragraph{The withheld era, net of costs (N2).} The registered
net-of-cost battery ran on both withheld-era positive cells at the
adjudicating 25 bps both-pay register, and the lodged verdict
sentence has two mandatory parts. First, the out-of-sample deficit
\emph{survives}: $10\%$ gives
$\hat\Gamma_{\mathrm{net}} = +0.00015 \pm 0.00010$ and 1958--89
gives $+0.00020 \pm 0.00017$, both positive-significant (the latter
barely). Second, the D3 \emph{amplification} clause is
\emph{refuted} out-of-sample: net sits below gross in both cells
($-24\%$ and $-7\%$) and fades to n.s.\ at the 50/100 bps tiers,
where the modern batteries strengthened. The mechanism's absence is
visible in the turnover telemetry: the modern amplification came
from the monolith out-churning the pinned oracle in crisis windows,
while on the withheld era the two churn equally (A1 $1.10$ vs.\ A9
$1.13$ turnover per day---the oracle slightly \emph{more}), so costs
subtract a little from $\hat\Gamma$ instead of adding.
Net-of-cost amplification is therefore an era-local phenomenon of
1990--2025, not a property of the deficit, and every ``costs
amplify'' sentence in this paper is scoped accordingly. And the
era-scoped economic verdict is weaker still, stated wherever this
result is cited: 1926--89 effective costs sat well above 25 bps
(50--100 bps is the plausible band), and at those era-realistic
tiers the two-arm deficit does \emph{not} survive---%
$\hat\Gamma_{\mathrm{net}}$ is n.s.\ at 50 bps
($+0.000102 \pm 0.000110$; $+0.000182 \pm 0.000198$) and at 100 bps
($+0.000005 \pm 0.000150$; $+0.000150 \pm 0.000280$)---so the
registered 25 bps significance rests on a cost level anachronistic
for the era it is applied to, and we do not present it as the era's
economic verdict. The counterpoint, at the same prominence
(descriptive, same JSON): the pool's A6-over-A1 \emph{net} edge is
significant at every tier in both cells---the retention system's
net superiority survives era-realistic costs even where the
two-arm deficit does not (A1$-$A6
${\approx}{+}10.6$ and ${+}11.3$ bps/day net of 25 bps, growing
with tier), carried by
A6's turnover discipline ($0.71$ vs.\ $1.10$)---consistent with the
D3/H batteries, not scored (\texttt{results/e\_prewar\_costs.json}).

\begin{figure}[t]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/fig10_signrule.pdf}
    \caption{The sign-rule scorecard: $\hat\Gamma$ (mean $\pm$ 95\% CI)
    for the walk-forward cells of all three market histories (US
    1990--2025, US 1926--1989 with its sub-eras, Japan 1990--2025)
    plus the 4H crypto gate cell; the NBER-labeler and stitched cells
    of the inclusive scorecard are tabulated in the released JSONs.
    Color and shape encode the observed outcome
    (full pre-registered ordering / flat / inversion, classified by the
    pre-stated rule). Positive deficits coincide with orderings and
    negative deficits with inversions in both directions and both
    hemispheres; the annotated cell
    ($12\%$ withheld: negative-significant $\hat\Gamma$, flat table) is
    the sign rule's one out-of-sample \emph{miss}---distinct from the
    inclusive scorecard's strong-form-only misses, which are weak-form
    hits, not rule violations. Walk-forward intervals are
    implementation precision
    (seed noise) conditional on each window's single history.}
    \label{fig:signrule}
\end{figure}

\paragraph{What tracked the outcomes.} Across the two 1990--2025
walk-forward tests, the sign of $\hat\Gamma_{\mathrm{forget}}$ tracked
the outcome direction both times; gate 1's pooled criterion tracked
neither, and on the withheld era its strongest pass coincided with an
inversion. When the sign rule was first formulated it was
\emph{postdictive}---written after seeing both 1990--2025 outcomes,
sharing arm A1 with the contrasts it prices (its non-circular content
is that A9 sides with the retaining arms exactly when the deficit is
positive)---and we said so. The withheld era was its first
out-of-sample exposure, in both directions, and it scored 5 of 6;
Japan, its first exposure outside the US (below), scored 4 of 4
under a lodged region-specific sign prediction.
The rule's standing therefore changes register: no longer merely a
lodged hypothesis, not yet a validated deployment rule---the record
is two countries, aggregate cross-sections only, one labeler family,
and the miss at $12\%$ is real. The forward test is unchanged and
pre-committed: $\hat\Gamma$'s sign prices the pool on CRSP
constituents, with the registration lodged externally (OSF) before
any data access, alongside the NBER-labeler forward test whose
specification was lodged before NBER dates were ever joined to a
panel.

\paragraph{The first temporal-custody cell, resolved.} The NBER
forward test ran immediately after its external lodging (registration
deposited with the Open Science Framework at 22:47 ET, 2026-07-15;
NBER dates first joined to the panel at 22:55 ET), and it is the one
cell in this paper whose ex-ante status a third party can certify. The
labeler has \emph{no free threshold anywhere}: regime = NBER recession
state $\times$ 50-day trend, with the causal primary variant using
only announcement-lagged information (a recession enters the labeler
on the date the NBER committee \emph{announced} it, verified against
the committee's published announcement records). Recession-cell
dormancies are finally crisis-scale by construction (up to $2{,}557$
trading days---the 1992${\to}$2001 and 2010${\to}$2020 gaps). The
structure screen, one descriptive line per its demotion: null on
both variants ($z = 1.74$ primary; pooled edges conditioning)---the
third independent dissociation of the screen from
pool value---while the deficit prices positive:
$\hat\Gamma = +0.00081 \pm 0.00033$ on the causal walk-forward,
strengthening to $+0.00105 \pm 0.00033$ net of 25 bps crisis-window
costs (the cost-amplification signature, reproduced ex ante on a
labeler the cost battery never saw; turnover $1.04$ vs.\ $0.63$/day),
with the ordering present exactly as the rule demands (A6 $<$ A1 at
Holm $1.6\times10^{-4}$ gross; the full A6 $<$ A5 $<$ A1 chain at
Holm $\le 3.1\times10^{-5}$ net, and A6 beating the pinned oracle at
$1.7\times10^{-3}$ net). Under the lodged scoring this cell is a
\textbf{hit under both the weak and the strong forms}---and it holds
the stronger event-level standing of the two K1 cells, passing both
registered tests (the event-level paragraph above). One scoring
choice is confessed rather than buried: no registration fixed whether
the gross or the net register adjudicates the strong-form
chain---gross, A6-vs-A5 is flat (Holm $0.94$) and the full chain holds
only net of costs, the economically primary register by the cost
battery's own argument, but an argument that predates this cell rather
than this scoring decision. Scored gross-only, the cell is a weak-form
hit and a strong-form partial; the amendment lodged for the CRSP test
fixes the net register in advance so this ambiguity cannot recur.
Disclosures at
equal prominence: the NBER stitched variants are collapsed to the
standing stitched caveat---schedule resampling cannot reproduce
multi-year dormancy, and their full tables live in the released
JSONs---entering the scorecard as one both-forms hit (the primary,
flat with $\hat\Gamma$ n.s.) and one weak-form hit/\emph{strong-form
miss} (the non-causal calendar-dated variant, the same pattern as
the withheld $12\%$ cell); and the registration's LORO and era-blocked slices,
initially owed, are now run
(\texttt{results/e\_french49\_nber\_loro.json}) with a mixed
disposition we state in full: the deficit survives every one of the 20
single-event exclusions and all calendar exclusions
(drop-all-of-2020: $+0.00042 \pm 0.00026$; drop-2008/09:
$+0.00036 \pm 0.00030$)---so it is not single-event-carried---but its
mass concentrates in the two deep-dormancy recession-entry
reactivations (January 2009, dormancy $1{,}378$ days, and June 2020,
dormancy $2{,}445$ days; dropping the former alone cuts
$\hat\Gamma$ to $41\%$ of headline), and the 2010s era block is
\emph{negative}-significant for both contrasts. The cell is therefore
event-robust in the strict sense but two-recession-concentrated and
not era-consistent---materially weaker than the L3 cell's robustness
profile, and we say so. The interpretable reading, flagged as
interpretation: the 2010s block contains no recession-cell
reactivations at all (no NBER recession reactivated in that decade's
evaluated window), so it consists entirely of expansion-cell events
where retention has nothing to protect and pays premium without
claims---concentration in long-dormancy recession entries is arguably
the registered phenomenon itself, but era-consistency cannot be
claimed. Because the labeler carries no threshold,
this result also adjudicates the registered fragility discriminator:
the granularity-window reading of the L3 threshold sensitivity gains,
and the fixed-cutoff artifact reading loses its remaining support.

\paragraph{The live measurement-split commitment (ledger row 11).}
One forward test is already running against data that did not exist
at the freeze. On 2026-06-30 both diagnostic cells were frozen on
the vendored panel (which ends 2026-05-29): the NBER cell at
$\hat\Gamma = +0.00081 \pm 0.00033$ ($+8.1 \pm 3.3$ bps/day) and the
drawdown L3@$15\%$ cell at $+0.00091 \pm 0.00020$
($+9.1 \pm 2.0$ bps/day), both positive-significant
(\texttt{results/temporal\_freeze\_2026-06-30.json}, mirrored in the
OSF custody record). The outcome window is 2026-07-01 through
2027-03-31, scored on 2027-04-07 under the lodged rule: a positive
frozen diagnostic predicts A6 over A1 on the window's qualifying
reactivations, and the cell is \textbf{VOID} if no qualifying
reactivation occurs. Nothing in this paragraph can be revised by us
after the fact; it is the cheapest fully temporal test the program
owns while the CRSP battery waits on data access.

\paragraph{The first non-US cell: Japan (E-R region 1).} The lodged
regional register (E-R, a fixed region order with region-specific
sign predictions lodged before any regional data was downloaded)
opened with Japan: the 25 Japanese size--value portfolios (daily,
value-weighted, 1990--2025, zero drops), run under the byte-identical
frozen L3-at-$15\%$ battery. The lodged region prediction---%
``$\Gamma_{\mathrm{Japan}} \approx 0$ or negative,'' reasoning from
Japan's short post-bubble crisis dormancy---\textbf{hit, on the
negative side}: $\hat\Gamma = -0.00060 \pm 0.00016$
(negative-significant, $n_{\mathrm{react}}{=}26$), with the predicted
inversion realized Holm-strong---A1 $0.00947$ beats A5, A9, and A6
$0.01015$; A6-vs-A1 Holm $3.9\times10^{-8}$---the largest real-data
inversion magnitude in the program ($-6.0$ bps/day post-reactivation):
the retention-plus-invariance premium as a pure visible cost, exactly
where the sign rule says it should be. The cell is flagged
\textbf{occupancy-anomalous} in the same sentence as its verdict:
the crisis union occupies $67.2\%$ of Japanese trading days at the
$15\%$ threshold, so crisis is the \emph{majority} state and the
rare-regime premise of the whole mechanism fails by inversion---with
the custody grade stated honestly: the occupancy-anomaly scoring
clause was written after Japan's results were seen (post-hoc for
Japan; lodged ex-ante for the four remaining regions). All three
sweep cells are sign-rule-consistent across thresholds, under both
forms ($10\%$
$\hat\Gamma = -0.00002 \pm 0.00009$, n.s.\ with a flat family;
$12\%$ and $20\%$ $\hat\Gamma = -0.00029 \pm 0.00012$ and
$-0.00048 \pm 0.00020$, with
inversions at Holm $1.4\times10^{-6}$ and $3.5\times10^{-6}$),
bringing the out-of-sample sign-rule record to 9 of 10 cells across
the withheld era and Japan, both directions represented. The
structure screen is one descriptive line, per its demotion:
$z = -0.90$, conditioning below pooled---a fourth independent
dissociation of the screen from pool value
(\texttt{results/e\_japan\_L3\_dissoc.json}).

\paragraph{Position in the finance and continual-learning literatures.}
Crisis-conditional strategy behavior is a documented finance
regularity---momentum profits condition on market state
\citep{cooper2004market}, crash in panic states defined by
drawdown-plus-volatility indicators close to our L3 labeler
\citep{daniel2016momentum}, and short-horizon predictability
concentrates in bad times \citep{rapach2010out, henkel2011time}---but
that literature conditions a strategy's \emph{expected return} on the
state, whereas E1f measures whether the state's \emph{dormancy erodes a
bounded learner's parameters}: a retention counterfactual
($\hat\Gamma_{\mathrm{forget}}$) that is defined and decision-relevant
even where conditional returns are flat, and whose econometric ancestor
is the post-break estimation-window question
\citep{pesaran2007selection} rather than state-dependent alpha. The
substrate is industry-momentum territory
\citep{moskowitz1999industries}, and the canonical warning that
industry-level effects need not survive disaggregation
\citep{grundy2001understanding} is one more reason the CRSP constituent
test decides the interpretation. In continual-learning terms,
$\hat\Gamma_{\mathrm{forget}}$ is a decision-regret backward-transfer
measure taken against a multi-head oracle \citep{lopez2017gradient,
chaudhry2018riemannian}; unlike replay remedies that spend memory to
suppress forgetting unconditionally \citep{rolnick2019experience,
chaudhry2019tiny}, the two-gate rule asks first whether forgetting is
worth buying at all---and the replay-equipped monolith, whose buffer
is not coupled to head eviction, has now been run as the natural
competing baseline in both settings: it buys most of the deficit and
still loses to the pool (this section and Section~\ref{sec:e1}).
Finally, the persistent, non-decaying form of the deficit is the
learner-side analogue of slow-moving capital
\citep{duffie2010presidential, mitchell2007slow}: there the capital,
here the training resource, follows recent reward and therefore
arrives at the dislocation late---and the cost battery's finding that
the deficit \emph{strengthens} net of crisis-window costs is that
literature's signature, measured in an allocator's parameters rather
than in prices.

\paragraph{Caveats at the same prominence as the result.} (i) Each
walk-forward design shares one market history per window; seeds vary
arm initialization only (the deterministic A8a arm has a zero-width
CI), so its intervals quantify implementation noise \emph{conditional
on that history}, not sampling uncertainty across markets. The
record now spans three independent market histories---US 1990--2025,
US 1926--1989 (its sub-eras are slices, not new histories), and
Japan 1990--2025---with the NBER labeler a second lodged test on the
first, the same independence rule Addendum O's pooling uses; but
every cell is still a single realized history of a country's
aggregate cross-section. (ii) The stitched counterparts no longer
simply support the null: at 100 seeds their $\hat\Gamma$ turns
marginally positive under both labelers while their arm families stay
flat (the wrinkle disclosed above)---still consistent with two
readings we cannot separate: the walk-forward precision is
single-history optimism, or stitching destroys precisely the
long-dormancy, cross-block structure that makes retention pay (the L3
crisis inventory is 11 blocks; stitched schedules cannot reproduce a
2{,}953-day dormancy by construction). The CRSP panel, which supports
walk-forward evaluation over multiple disjoint eras, is the
discriminating experiment. (iii) L3 was designed after the
L1/L2 gate results---with full knowledge of which property (dormancy
scale) had killed the first two labelers---and pre-registered before
its own runs; under a further Bonferroni $\times 3$ over the labeler
family, at 100 seeds every primary dissociation claim survives (all
$\le 4.5\times10^{-12}$ corrected, including A6-vs-A9), as do the
L1 inversion's three pairs (headline A1$<$A6 corrected
$3.7\times10^{-26}$). (iv) Transaction costs are no longer deferred on
this substrate: the L3 deficit and the L1 reversal are measured under
modeled crisis-window costs (25--100 bps, both-pay), and the two
withheld-era positive cells across the same tiers (surviving only
at 25 bps---anachronistic for that era---without the modern era's
amplification, N2 above). What
the cost
model does not include---market impact, borrow, capacity---and every
other substrate in the paper remain gross; the deflated-Sharpe/SPA
battery stays reserved for any alpha claim
(Section~\ref{sec:stats}). (v) The threshold window is a
two-register statement: at zero hysteresis each era's deficit lives
at essentially one threshold and the paying point moved
($15\% \to 10\%$) with crisis density, while the lodged E-X2 map
adjudicates a \emph{window} on both eras (overlapping $13$--$16\%$
under exit hysteresis, superseding the relocation
reading)---specification robustness in the naive
sense remains refuted (the prewar window exists only under
hysteresis and its lodged $10\%$ anchor is isolated), any deployment
must still locate its era's window, and the one committed estimator
for
doing so has already failed its first calibration (the $\kappa$
infeasibility above), so no validated location procedure currently
exists. (vi) The evaluated reactivation
inventory behind $\hat\Gamma > 0$ is 27 events (deterministic across
seeds; the evaluated half begins in 2008), right-skewed in
contribution---the early-COVID windows, 2025-04, 2015-11, and 2011-11
contribute disproportionately, and about a third of events have
individually negative $\hat\Gamma_i$. Under leave-one-reactivation-out
(\texttt{results/e\_french49\_L3\_loro.json}) the deficit survives
every single-event exclusion (range $[0.00071, 0.00101]$, all
significant), survives excluding the entire 2020 calendar year
($0.00080 \pm 0.00019$; the two overlapping COVID probe windows share
six days, so the calendar exclusion is the clean test), and is
independently positive and significant within each era with a
meaningful sample (2010s: $0.00077 \pm 0.00022$, $n{=}14$; 2020s:
$0.00094 \pm 0.00040$, $n{=}12$), with the same pattern for the joint
A1$-$A6 contrast. Crisis reactivations contribute more than calm
ones---as the mechanism predicts---but no single event, nor all of
2020, accounts for the effect (within the single-history frame of
caveat (i), which these analyses cannot lift).

\subsection{E2 --- capacity sweep: when does the dissociation close?}
\label{sec:e2}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/fig3_capacity.pdf}
    \caption{E2: post-reactivation regret vs.\ per-specialist capacity $K$
    ($R = 4$; 100 seeds; left: hard LRU, right: soft interference).
    Reward-independent arms are flat in $K$; reward-driven arms close the
    gap only as $K \to R$ under hard slots, and never fully close it under
    interference.}
    \label{fig:capacity}
\end{figure}

Sweeping per-specialist capacity $K = 1, \dots, 4$ ($= R$) under both
memory models (Figure~\ref{fig:capacity}): \nm{} arms are \emph{flat in
$K$} (A6: $0.0308$ at every $K$; A5: $0.0324$)---a converged one-per-regime
assignment needs one slot per specialist, and extra capacity is simply
unused. Under the hard model the reward-driven arms improve monotonically
with $K$ and approach the retaining class only at $K = R$ (monolith:
$0.0339 \to 0.0322$, matching A5 at $K{=}4$; router likewise), where
eviction becomes unnecessary and the allocation question dissolves. The
soft (interference) model inverts the diagnosis instructively: with no
eviction, capacity slots stop binding and the reward-driven arms are
\emph{flat} in $K$ at $\approx 0.0327$---gentler than hard eviction at
tight capacity ($0.0339$ at $K{=}1$ and $K{=}2$) but never
closing the gap to A6
($0.0308$) at \emph{any} capacity, because graded interference from
foreign updates erodes dormant heads no matter how many slots exist.
Slack capacity is thus one audited boundary of the contribution, with a
caveat that depends on the memory physics: with $K \ge R$ \emph{and}
hard isolation, conditioning alone suffices (the
regime-switching-finance baseline is vindicated exactly there); with
shared representations (the realistic reading for parametric models),
interference keeps the population's structural protection valuable at
every capacity.

\subsection{E3 --- dormancy sweep and the break-even window}
\label{sec:e3}

\begin{figure}[t]
    \centering
    \begin{subfigure}{0.49\textwidth}
        \includegraphics[width=\linewidth]{figures/fig4_dormancy.pdf}
        \caption{Dormancy sweep (E3).}
        \label{fig:dormancy}
    \end{subfigure}\hfill
    \begin{subfigure}{0.49\textwidth}
        \includegraphics[width=\linewidth]{figures/fig5_heterogeneity.pdf}
        \caption{Episode-heterogeneity sweep (E4).}
        \label{fig:het}
    \end{subfigure}
    \caption{\textbf{(a)} Post-reactivation regret vs.\ dormancy $D$ of the
    focal regime: the reward-driven arms' deficit saturates once $D$
    exceeds the eviction scale (Lemma~\ref{lem:eviction}); levels are not
    comparable across $D$ (schedules differ), arm gaps within each $D$
    are. \textbf{(b)} Fixing the retaining allocation and sweeping episode
    heterogeneity isolates the invariance axis: the ERM$-$INV gap grows
    $2.6\times$ across the sweep.}
    \label{fig:e3e4}
\end{figure}

Controlled schedules place the focal regime's reactivations after dormancy
$D \in \{21, 63, 126, 252, 504\}$ days (Figure~\ref{fig:dormancy}). The
retention gap (A1 $-$ A6 post-reactivation) grows from $0.0011$ at
$D = 21$ to $0.0034$--$0.0045$ for $D \ge 63$ and saturates---the
signature of Lemma~\ref{lem:eviction}: once dormancy exceeds the eviction
scale ($\approx K$ foreign-regime acquisitions, here a few dozen days),
the monolith's head is gone with probability $\approx 1$ and longer
dormancy adds no further loss, while the \nm{} owner's deficit is flat at
zero by construction. The recent-performance allocator degrades fastest
(its capital floor keeps eroding the dormant specialist throughout).
Feeding the measured quantities into Proposition~\ref{prop:breakeven}
(relearning cost $C_{\mathrm{relearn}} \approx 0.004$ per probe day over
$N_p = 15$ days; writing the carrying cost as a fraction $f$ of the
per-day probe saving $C_{\mathrm{relearn}} V$, the window condition reduces
to $f D \le \Phi(D) N_p$): the lower endpoint is the eviction scale at
which $\Phi$ saturates---E3 measures $\Phi(21) \approx 0.27$ and
$\Phi(63) \approx 0.86$, so $D_{\min} \approx 60$ days---and the upper
endpoint is $D_{\max} = N_p / f$. At $f = 1\%$ retention pays for $D$
between roughly $60$ and $1{,}500$ days; at $f = 10\%$ the window shrinks
to $D \in [\approx 60, \approx 150]$ days---and at carrying costs above
$f^{*} = N_p \max_D \Phi(D)/D \approx 20\%$ of the probe saving, \emph{no}
dormancy length pays. The
existence of the \emph{upper} break-even is the practically honest point:
idle desks are not free, and a fund whose crisis playbook reactivates
once a decade may rationally let it decay if maintaining it costs enough.

\subsection{E4 --- episode heterogeneity isolates the invariance axis}
\label{sec:e4}

Holding the allocation fixed (emergent, retaining) and sweeping the
episode-heterogeneity scale (the spurious loading's standard deviation
relative to invariant signal) from $0$ to $2\times$ baseline
(Figure~\ref{fig:het}): the ERM specialist degrades steadily
($0.0299 \to 0.0378$ post-reactivation) while the invariant specialist
degrades about $20\%$ slower ($0.0289 \to 0.0352$; equivalently, ERM
degrades $25\%$ faster), so the A5$-$A6 gap
widens monotonically from $0.0010$ to $0.0026$ ($2.6\times$)---the regime-indexed analogue
of the Inv-PnCO heterogeneity result, and direct evidence that the
invariance margin is purchased against episode variability specifically
(at zero heterogeneity the objectives nearly coincide; the small residual
gap is the variance penalty acting as a regularizer against the sixteen
distractor features). Reactivation is an out-of-distribution event
\emph{within} one's own regime to precisely the degree that episodes
differ; retention solves dormancy, and this axis is what remains after.

\subsection{E5 --- ablations}
\label{sec:e5}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.98\textwidth]{figures/fig8_ablations.pdf}
    \caption{E5 ablations (post-reactivation regret, 100 seeds).
    \textbf{Left:} the invariance objective is U-shaped in $\beta$ and the
    variance-only control ($\beta = 0$) fails below retained ERM.
    \textbf{Center:} robustness to the competition window $W_c$ (the
    revised batching prediction, Remark~\ref{rem:batching}).
    \textbf{Right:} pinning the converged assignment matters.}
    \label{fig:ablations}
\end{figure}

\textbf{$\beta$ sweep, and the variance-only control fails as predicted.}
Post-reactivation regret is U-shaped in the mean-loss weight:
$\beta = 0$ (variance only) gives $0.0357$---\emph{worse than retained
ERM} ($0.0324$)---confirming in the decision setting the Inv-PnCO
finding that the variance term alone is degenerate (a head can achieve
zero episode-variance by being uniformly bad); the plateau
$\beta \in [0.25, 4]$ gives $0.0308$--$0.0318$; very large $\beta = 16$
degrades to $0.0351$---worse than the retained-ERM arm itself
($0.0324$), which is the honest reading: $\beta$ multiplies the
effective gradient magnitude, so $\beta = 16$ is not ``clean ERM
recovered'' but an unstably large step on the mean term (A5 remains the
correct $\beta \to \infty$ reference, trained at the proper rate). The
objective needs both terms, and is insensitive to $\beta$ across an
order of magnitude.

\textbf{Competition-window sweep.} $W_c \in \{1, 5, 20, 60\}$ days yields
$0.0306$, $0.0306$, $0.0308$, $0.0310$: flat, with per-step competition
marginally \emph{best}---the revision of our pre-registered batching
expectation discussed in Remark~\ref{rem:batching}. The mechanism's
robustness to its main scheduling hyperparameter is itself a deployment
asset.

\textbf{Niche bonus and pinning.} Competition alone ($\lambda = 0$)
reaches $0.0318$ versus $0.0308$ with the default shaping
($\lambda = 0.3$)---the bonus accelerates but does not cause the
assignment, consistent with GAUSE's $\lambda$ ablation. Disabling pinning
costs $0.0325$ versus $0.0308$: keeping competition perpetually open
re-admits exactly the reward-coupling the mechanism exists to remove,
though the EG dynamics' inertia limits the damage.

\subsection{E6 --- audit: when retention is worthless}
\label{sec:e6}

\begin{figure}[t]
    \centering
    \includegraphics[width=0.55\textwidth]{figures/fig6_snr_audit.pdf}
    \caption{E6 audit: the relearning penalty (post-reactivation regret
    relative to own steady state) as the signal-to-noise multiplier
    sweeps $0.5\times$--$8\times$. The monolith's relative penalty grows,
    but its absolute steady level collapses; the \nm{} advantage inverts
    near $2$--$3\times$ (see text).}
    \label{fig:snr}
\end{figure}

This experiment exists because our first implementation refuted us. At the
default parameters of an early prototype (signal-to-noise an order of
magnitude above realistic daily cross-sections), \emph{no arm separated
from any other}: the monolith relearned an evicted regime within two to
five days, the probe window scored mostly converged behavior, and
retention was demonstrably worthless. Rather than discard the parameter
regime, we promoted it to an audit axis. Sweeping the signal-to-noise
multiplier over $\{0.5, 1, 2, 4, 8\}$ (Figure~\ref{fig:snr}):

\begin{itemize}
    \item The monolith's relearning penalty (post-reactivation over steady
    regret) grows from $1.26\times$ to $2.01\times$ with SNR---relative
    relearning cost rises---but its \emph{absolute} steady level collapses
    ($0.0295 \to 0.0165$), because strong signal is refit almost
    instantly and then exploited deeply.
    \item The \nm{} advantage over the monolith is $+6.5\%$ at
    $0.5\times$, $+9.1\%$ at baseline, $+4.9\%$ at $2\times$---then
    \emph{inverts}: $-18.8\%$ at $4\times$ and $-60.0\%$ at $8\times$. At
    high SNR the episode-specific loading is itself strongly exploitable,
    and an ERM monolith that greedily refits each episode beats an
    invariant specialist that on principle refuses to chase it. The
    skyline arm inverts identically (A9 tracks A6), confirming this is a
    property of the \emph{regime}, not of our assignment mechanism.
\end{itemize}

The audit therefore draws the mechanism's boundary as a measured
crossover, near $2$--$3\times$ our baseline calibration: \nm{} pays where
signals are weak enough that relearning is slow and episode-chasing does
not compensate---which is the empirically realistic regime for daily
cross-sectional return prediction---and is counterproductive where
within-episode signal is strong, fast to fit, and worth chasing. This is
the decision-domain analogue of GAUSE's stale-specialist result under
intra-regime drift: reward-independent retention is an asset under
dormancy and a liability under exploitable drift, and the honest
deliverable is the location of the boundary.

One scope note belongs here rather than only in the limitations: the inversion documented in this section is established for the linear specialist class; under the shared-trunk gradient-descent replication of Section~\ref{sec:e1m} the high-SNR comparison diverges (the deficit grows rather than inverts, with the single-readout caveat discussed there), so the E6 boundary should be read as a property of the linear setting until the neural sweep is architecture-matched.


\subsection{Statistical protocol and what we deliberately do not claim}
\label{sec:stats}

All headline comparisons are Welch tests over 100 independent seeds with
Holm--Bonferroni correction within each experiment's pre-registered pair
family. At 100 seeds: the controlled batteries E1--E6, the stitched
daily panel, the synthetic replay battery (D1), the MLP battery of
Section~\ref{sec:e1m} (its SNR sweep at 20), the K2
expanding-window/replay battery, and---per the
pre-registered parity battery D6---the French L1/L3 walk-forward and
stitched batteries of Section~\ref{sec:e1f}. At 20 seeds as run: the
intraday retest of Section~\ref{sec:e1r}, the assignment diagnostic,
and the French cost (D3), mechanism-probe (X1/X3), withheld-era
(E-F), decision-layer (K3), and Japan (E-R) batteries; the D8
probe/dormancy grid at 10 seeds; the E-X2 threshold maps at tier-1
10 seeds with tier-2 four-arm re-reads at 20 (run where tier-1 CIs
exclude zero). The K1/N1/O event-level values come from instrumented
A1/A9 re-reads that reproduce the published per-seed raws exactly
(max $|$diff$|$ $= 0$, asserted in-script), and the N2 cost register
is a pure accounting pass over the same trajectories. One register
per cell: the 20-seed
D2 replay cell is superseded by its 100-seed K2 re-measurement
(Section~\ref{sec:e1f}), and no cell quotes both registers. In the walk-forward designs of
Section~\ref{sec:e1f} seeds vary initialization only, so those
intervals are conditional on a single history; tables report mean
$\pm$ 95\% CI. The synthetic and stitched
experiments expose every arm to identical data streams per seed (paired
design). We do \emph{not} report Sharpe ratios, deflated or otherwise, for
the synthetic headline---regret against a known optimum is strictly more
informative when ground truth exists---and we make no claim of live
trading profitability anywhere: on the panels that fail the gates such a
claim would be fitting noise, and on the equities panels
(Section~\ref{sec:e1f}) our numbers are decision-regret at portfolio
level---now measured \emph{net} of modeled crisis-window transaction
costs for the L3/L1 batteries (both-pay convention, 25--100 bps
tiers), gross elsewhere---which is still not a P\&L claim. For any future real-data deployment passing the gate, the
protocol of record is: deflated Sharpe \citep{bailey2014deflated} for any
P\&L claim, an SPA/Reality-Check test \citep{hansen2005spa, white2000reality}
across the arm family, transaction costs at $5/10/25$ bps with borrow,
nested walk-forward for every hyperparameter, and the
break-even-dormancy carrying-cost analysis of
Proposition~\ref{prop:breakeven}---pre-committed here.

\subsection{Synthesis}

Across E0--E6 the two-axis account survives every controlled test we
constructed against it, and E1m closes its last controlled loophole:
the deficit emerges under vanilla gradient descent with nothing
evicted. On real data its \emph{protocol} was refuted
twice and revised twice (E1r, E1f)---each time toward the
decomposition's own central term. What survives that record, stated
as the four claims we would defend: (1) the \emph{sign} of
$\hat\Gamma$ prices the pool---two in-sample tests, five of six
withheld cells, and four of four Japan cells (9 of 10 out-of-sample),
in both directions; a cell-level rule, distinct from the pooled
event-frequency claim, which is retired; (2) where the deficit is positive
it is real economics on the modern era---amplified by crisis-window
transaction costs on 1990--2025 under both labelers---while the
withheld era's cost verdict is split and disclosed: the two-arm
deficit survives only an era-anachronistic 25 bps register, but the
pool's net edge over the monolith survives every tier; the pooled
dollar register's CI covers zero, so the economic claim is
region-conditional by construction (Addendum O); the deficit is
collectible by replay on real data though the
invariance-equipped pool still beats replay on both substrates,
while an expanding-window retrainer collects only ${\approx}59\%$
and leaves a real $+3.7$ bps/day residual;
(3) its mechanism on real data is honestly open---the measured
quantity behaves as a regime-conditional \emph{allocation} deficit,
while eviction-forgetting proper remains a controlled-setting result
(E1, E1m); (4) gate 1 does not price the pool and is demoted to a
structure screen. Retention is
an allocation property: every isolated covering arm retains
(A4--A6, A9, A10), every reward-chasing arm relearns (A1--A3, A8b), the
boundary is crisp ($p \sim 10^{-23}$ for the class separation), and no
training objective crosses it (A7). Nor is the boundary about data
access: the expanding-window retrainer A1e discards nothing and sits
outside A1's class, yet on the substrate where the deficit is
positive it still pays a real residual
(Section~\ref{sec:e1f})---what defines the class is whether the
\emph{capacity assignment} chases reward, not what the training set
remembers. Invariance is an objective property:
it pays only inside a retaining allocation (super-additive interaction,
CI excluding zero), grows with episode heterogeneity (E4), requires
both of its terms ($\beta$ sweep)---and is honestly bounded: it is
absent under the shared-trunk MLP (E1m). The mechanism needs its preconditions:
bounded capacity (E2), real dormancy (E3), weak-signal relearning (E6),
and verifiable regime structure (E0/E1s)---each audited with the
experiment that locates its boundary. Where all preconditions held, the
emergent system matched the hand-designed skyline without being given the
assignment; where any failed, the system honestly bought nothing.

%=============================================================================
\section{Discussion}
%=============================================================================

\paragraph{Why two mechanisms, not one.} The temptation in both
literatures is monism. Continual-learning work treats non-stationarity as
forgetting and reaches for retention; OOD-generalization work treats it as
shift and reaches for invariance. The financial setting forces the
synthesis because its non-stationarity is genuinely two-layered: regimes
\emph{recur} (so there is something worth retaining) and recurrences
\emph{differ} (so retention alone retains the wrong thing). The
super-additive interaction is the empirical signature that the layers are
real: each mechanism's value is contingent on the other having done its
job. We conjecture this structure---retain the niche, generalize within
it---is generic to any domain with recurring-but-drifting modes: climate
regimes, epidemic waves, demand seasons, adversary playbooks.

\paragraph{The allocator, not the org chart.} Multi-strategy funds already
silo desks by regime---the practitioner instantiation of
reward-independent assignment. \nm{}'s contribution to that practice is
not the silo but the analysis of what destroys it: recent-performance
capital allocation \emph{across} silos (our A3, the industry's default)
erodes dormant desks at a rate set by its capital floor, and a learned
router---the modern ML upgrade---fails identically and for the same
structural reason (Proposition~\ref{prop:dichotomy}). The emergent
mechanism additionally shows the silo map need not be designed: at
$N \gtrsim R$ it is recovered automatically, matching the hand-pinned
skyline, with graceful degradation documented when competition's
preconditions weaken (the unpinned crisis niche of Section~\ref{sec:e1}).
The D4 probe sharpens the carrier attribution: keeping every silo's
research alive while letting recent-performance capital route the
serving decision (A3$'$, Section~\ref{sec:e1}) is strictly worse than
the fully reward-coupled default---the deployment coupling, not
training starvation, is the destructive half.

\paragraph{What the diagnostic-first protocol buys.} The most consequential
result in this paper for applied work is the pairing of a null and its
mirror: where the diagnostics refuse to certify structure or forgetting,
the machine run anyway goes flat or inverts (E1s, E1r, E1f-L1, the
withheld 15\% cell)---and wherever the measured deficit is positive,
the same machine, unchanged, produces the pre-registered ordering
(E1f-L3 in-sample; the withheld 10\% and 1958--89 cells
out-of-sample). The
protocol converts ``our regime-aware method beat baselines on a
backtest'' (unfalsifiable in the presence of label noise and selection)
into separable, individually falsifiable claims: \emph{structure exists
here} (E0, cheap, runs before any method), \emph{dormancy actually
destroys something} ($\hat\Gamma_{\mathrm{forget}}$, one counterfactual
arm), and \emph{given both, the mechanism captures it} (E1). We commend
the split to the regime-aware finance literature at large.

\subsection{Claims ledger}
\label{sec:ledger}

Every headline claim, its evidence grade, and what would refute it, in
one place; rows ordered strongest to most contingent. Rows 1--6 are
settled within their stated scope; rows 7--10 are the frontier; rows
11--12 are the lodged bets.

\begin{table}[h]
\centering
\scriptsize
\begin{tabular}{p{0.30\textwidth}p{0.30\textwidth}p{0.30\textwidth}}
\toprule
\textbf{Claim} & \textbf{Evidence (status)} & \textbf{What would refute it} \\
\midrule
1. Two orthogonal axes; 2$\times$2 dissociation & 100 seeds, joint $p{=}6.0{\times}10^{-23}$; TOST'd equivalences (\emph{established, controlled}; D5 quantile test failed---the bound's tail reading demoted to unconfirmed, disclosed) & a shared-backbone setting collapsing one axis \\
2. Super-additivity: invariance inert in reward-driven allocation & $I$ CI excludes 0; A7$\approx$A1 TOST $p{=}9.6{\times}10^{-29}$ (\emph{established, controlled}) & A7 helping over A1 anywhere \\
3. Emergent assignment recovers the oracle map unseen & A6$\approx$A10 $p{=}0.53$; covering 20/20 seeds (\emph{established}; crisis niche unpinned, disclosed) & non-coverage at $N \gtrsim R$ \\
4. Interference emerges under gradient descent, nothing evicted & $\Gamma_{\text{mlp}}$ excess $+0.0075\pm0.0004$, $p{=}1.9{\times}10^{-78}$ (\emph{established}; invariance null under MLP, disclosed) & the gap vanishing at matched architecture \\
5. Retention inverts at high SNR (linear class) & E6 sweep; oracle inverts identically (\emph{established, linear}; MLP diverges, disclosed) & inversion reproducing nowhere outside E6 \\
6. Structure screen is demoted: it does not price the pool & four dissociations incl.\ $z{=}23.5$ pass with inversion and Japan's screen-fail-with-priced-pool (\emph{established by record}) & the screen tracking pool value out-of-sample \\
7. The deficit is real economics where positive & modern era: cost-amplified $+0.00126 \to +0.00230$ net, both labelers. Withheld era: gross yes; pool-vs-monolith net yes at every tier; two-arm $\hat\Gamma_{\mathrm{net}}$ only at the era-anachronistic 25 bps, disclosed. Pooled dollar register CI covers 0 $\Rightarrow$ economic claim region-conditional (Addendum O). Replay collects it, expanding window leaves a real residual, pool still wins; banded countermeasure run, net edge stands (\emph{frontier: aggregate, two countries}) & a disciplined implementation beyond $b{\le}2$ bands closing the net gap \\
8. Real-data mechanism = regime-conditional allocation deficit & X1/X3: no dormancy/rehearsal covariate; non-decaying (\emph{honestly open}) & a covariate or transient found; or CRSP contradiction \\
9. $\hat\Gamma$'s sign prices the pool (cell-level rule; distinct from the retired pooled frequency claim) & 12/16 strong, 16/16 weak (one-directional); 9/10 out-of-sample = withheld era + Japan; separately, one temporal-custody hit (NBER---same US-modern history, so outside the 9/10); dependence caveats stated (\emph{frontier}) & one clean refuting cell (definitions lodged) \\
10. The paying threshold is a window, not a point & lodged E-X2 criterion: WINDOW on both eras, overlapping $13$--$16\%$ under exit hysteresis ($h{=}0$ relocation $15\% \to 10\%$ superseded; prewar window hysteresis-only, its $10\%$ anchor isolated---PX2e miss); committed $\theta(\mathrm{era})$ estimator failed calibration and is retired pre-CRSP (\emph{no validated estimator}) & no window on CRSP under the lodged criterion \\
11. Live measurement-split prediction & frozen 2026-06-30, both cells positive; scored 2027-04-07 (\emph{lodged, unresolved}) & A1 outperforming A6 in the outcome window \\
12. CRSP T-split forward battery & OSF-lodged design, register fixed pre-data; window estimator retired pre-data, frozen-$15\%$ primary (\emph{lodged, unresolved}) & sign-rule miss under the T-split \\
\bottomrule
\end{tabular}
\caption{The claims ledger. Grades and refutation conditions are lodged
in the pre-registration record (osf.io/nsx4e; \texttt{PROVENANCE.md}).}
\label{tab:ledger}
\end{table}

%=============================================================================
\section{Limitations}
\label{sec:limitations}
%=============================================================================

\begin{enumerate}
    \item \textbf{The controlled study carries the causal claims; the
    real-data record is multi-era but still aggregate
    cross-sections from two countries.} The dissociation is demonstrated
    with full causal control only in synthetic markets. On real data,
    the record now spans 1926--2025 and two countries (US industry
    walk-forwards plus the Japanese size--value panel; sixteen scored
    cells, costs measured on the 1990--2025 US
    batteries and, at 25--100 bps, on the withheld-era positive
    cells), but every cell is portfolio-level and
    single-history; the
    crypto/commodity nulls and the L1/prewar/Japan inversions bound it, and
    the mechanism attribution on real data is open---allocation
    deficit versus eviction-forgetting
    (Section~\ref{sec:e1f})---with CRSP constituents and an
    NBER-anchored slow labeler as the pre-committed deciders. The
    constituent-level demonstration remains undone: the CRSP
    S\&P~500 panel (1990--2025, 2000/2008/2011/2015/2018/2020/2022 as
    crisis episodes) is the pre-committed forward test with its
    statistics fixed in advance (Section~\ref{sec:stats}), and it is
    where the sign rule, the aggregation caveat, and the mechanism
    question are decided. The paying threshold must be estimated per
    era---and the committed estimator for doing so failed its first
    calibration (disjoint $\kappa$ requirements across the two
    available eras), a failure disclosed before any CRSP access; the
    estimator is retired and the CRSP battery runs at the frozen
    $15\%$ with the threshold set as a descriptive map only.
    \item \textbf{Real universes remain modest.} Five crypto pairs,
    three commodities, and 49 (post-1990) or 36 (pre-1990) industry
    portfolios at daily frequency;
    the E0/E1s nulls are bounded to their panels, labelers, and feature
    inventories, and the E1f positives are bounded to one aggregate
    cross-section under one drawdown labeler family (threshold sweeps
    pre-registered in both eras).
    \item \textbf{Assumption~\ref{ass:hyper} idealizes episodes.} Real
    episodes are neither independent (macro memory links crises) nor
    exchangeable (secular drift in market microstructure), and crisis
    regimes offer 5--7 episodes at best---the $O(B/\sqrt{E_r})$
    estimation terms in Lemma~\ref{lem:transfer} are not decorative. The
    sub-episode and cross-asset mitigations of Section~\ref{sec:mechanism}
    enlarge the environment set but introduce their own dependence; we
    flag both rather than resolve them.
    \item \textbf{Theory register.} Propositions~\ref{prop:dichotomy}--%
    \ref{prop:additive} are idealized-model results: the reward-driven
    eviction rate assumes per-step foreign-update probability $p > 0$
    (verified by construction in our arms, plausible but unverified for
    arbitrary allocators), and we claim no adversarial-regret guarantee
    for the coupled winner-take-all dynamics. The bound's additivity
    motivates but does not entail the empirical super-additivity; we
    measure the latter.
    \item \textbf{The mechanism inverts under strong exploitable drift.}
    E6 is a limitation stated as an experiment: at high within-regime SNR,
    invariant retention loses to episode-chasing ERM by up to $60\%$. Any
    deployment must locate itself on the E6 axis first; a staleness
    trigger in the GAUSE style (re-opening competition on confident
    underperformance) has been piloted at 20 seeds
    (\texttt{results/e6\_trigger.json}): it tracks A6 where retention
    pays ($0.0307$ vs.\ $0.0307$ at baseline SNR) but does \emph{not}
    close the high-SNR inversion, so the remedy remains open. The
    boundary is established for the linear, regime-keyed model class:
    under the single-readout MLP of Section~\ref{sec:e1m} the
    high-SNR inversion does not reproduce (the lodged divergence,
    with its architectural caveat).
    \item \textbf{Capacity as a binding constraint is a modeling choice.}
    As in GAUSE, a purely tabular learner could hold all regimes; the
    contribution is conditional on capacity binding (true for fixed-size
    parametric models and for the operational reading---calibration
    attention, infrastructure---of strategy pools). The soft-interference
    results (E2) show the dissociation does not depend on the discrete
    eviction model.
    \item \textbf{Task-incremental scope.} Every arm receives the same
    causal regime label; the comparison is information-symmetric, but
    inferring regimes while specializing (class-incremental) is unsolved
    here. GAUSE's label-free variant collapsed under real feature overlap,
    and we expect the financial case to be at least as hard; we do not
    claim otherwise.
\end{enumerate}

%=============================================================================
\section{Conclusion}
%=============================================================================

We decomposed non-stationary financial decision error into two
independently controlled failure modes---between-regime forgetting, owned
by the capacity allocator, and within-regime drift, owned by the training
objective---and showed theoretically and experimentally that fixing either
without the other forfeits most of the available improvement, while
fixing both recovers the hand-designed skyline without hand design. The
enabling property, imported from GAUSE and ported to decision-focused
learning, is the isolation-with-coverage of the converged
assignment---obtained emergently through reward-independent pinning; the new
axis, imported from invariant predict-and-optimize and ported to recurring
regimes, is episode-variance penalization of a decision-focused loss; the
bridge between their guarantee languages is one Pinsker step. Around the
mechanism we built the evaluation standard we believe the area needs:
pre-registered orderings, a structure diagnostic that runs before claims,
audits that locate every boundary of validity, and the discipline of
reporting the predictions the data refused. When the regime returns, the
specialist who kept the playbook---and kept it general---decides best;
this paper says precisely when that sentence is true, and how to check.

\bibliographystyle{plainnat}
\bibliography{references}

\end{document}
